# 斐讯 R1 改造为 Home Assistant 语音卫星：技术分析与落地方案

> 编制日期：2026-08-27\
> 修订日期：2026-08-28\
> 输入需求：`../_codex-workspace/inbox/斐讯R1分析.md`\
> 目标规模：3～8 台斐讯 R1，Home Assistant OS 运行于 PVE 虚拟机\
> 原则：R1 端侧唤醒、HA 端中文识别与意图处理、本地优先云端兜底、优先免拆、R1 直连 Home Assistant

> 2026-09-07 仓库清理修订：当前源码 v63 移除旧 Assist WebSocket APK 服务及启动工具，保留原生链路；下文阶段 2 WebSocket 实施内容作为历史架构记录。历史原始产物位置与恢复方式见 [归档说明](test-results/README.md)。本次不改变阶段门槛、第 14 章指标或用户已暂停的验收排期。

## 1. 执行摘要

### 1.1 结论

这个项目**可做，但必须先完成首台R1真机开发，通过功能、数据、链路、安全、恢复和稳定性准入后才能考虑批量实施**。自然中文对话、持续/多轮对话、流式回复、主动播放、播放中唤醒和智能终止仍是交付功能；依赖非专业真人控制条件的声学效果验收按2026-09-12决定跳过而不宣称通过。

斐讯 R1 的硬件基础仍有利用价值：公开拆解显示其采用 RK3229、512 MB RAM、8 GB eMMC、四颗麦克风、AK7755 音频 DSP、独立高低音单元及 802.11ac/蓝牙模块。社区项目也证明 Android 5.1 上可以安装替代 APK、录音、播放和控制部分硬件。但以下三件事必须分开看：

1. **能安装 APK**不等于第三方 APK 能取得原厂四麦波束成形、AEC 和 DSP 的完整效果。
2. **能向 Assist Pipeline 上传音频**不等于设备会自动注册成原生 `assist_satellite` 实体。
3. **同时向多台设备发送播放命令**不等于多台设备严格同步，更不等于左右声道。

生产音频链路固定为：**R1 本地采集、AEC/NS/AGC、VAD 和 KWS 唤醒 → 唤醒后上传命令音频 → HA 中文 STT → HA 意图/对话/资源调用 → 中文流式 TTS 回传 R1**。R1 不运行完整中文 STT 或 LLM：RK3229 和 512 MB RAM 更适合轻量 KWS/VAD，中文识别集中在 HA 端更容易保证精度、升级和多设备一致性。

### 1.2 推荐架构

生产方案优先级如下：

1. **主方案：APK 实现端侧唤醒和 ESPHome Native API 语音卫星子集。** R1 空闲时本地运行 KWS，不持续上传家庭环境音；唤醒后携带预缓冲从 HA 管线的 STT 阶段开始。R1 被官方 ESPHome 集成发现并注册为 `assist_satellite`、`media_player` 和配置/诊断实体，不需要外置网关。
2. **备选方案：R1 APK + HA 内部自定义集成。** 协议可以自行控制，Android 端更容易开发；代价是每次升级 Home Assistant 都要维护自定义集成。
3. **快速闭环验证：APK 直接调用 Assist Pipeline WebSocket。** 用于在真机上尽快验证录音、STT、意图和 TTS，但它本身不会创建原生卫星实体，只作为原生协议开发前的阶段性验证路径。

不推荐把 Wyoming Satellite 作为新项目主方案：Wyoming 仍是 HA 连接 Whisper、Piper、openWakeWord 等语音后端的重要协议，但旧的 `wyoming-satellite` 项目已经归档，完整卫星能力正在转向 ESPHome 协议路线。

即使允许拆机，也不把“刷通用 Linux”设为默认路线。Android 本身已基于 Linux 内核；R1 的主要价值在于原厂已适配的四麦、AK7755 DSP、AEC、功放和高低音路由。拆机优先用于完整备份、获得 ADB/root 和恢复能力；生产主线保留原厂 Android 内核、Audio HAL、DSP 固件和校准数据，只替换上层语音服务。

2026-09-08 用户决策将原厂音频处理链提升为当前最高优先级：目标是让卫星实际消费设备内原厂四麦、MicArray、DOA、AEC、DSP和校准处理结果，而不只是保留文件或经过标准 Audio HAL。允许使用拆机、Root、补丁 boot、定制 recovery/system、专用 SELinux 策略、特权系统服务和刷机；完整 eMMC 备份、低层恢复入口及一次受控完整回刷是任何永久修改的前置门槛。标准 AudioRecord 与自研处理降为对照及最终后备，只有原厂路线全部形成可复现失败证据并经用户决定后才能采用。实施细则见[原厂四麦与音频调校优先路线](docs/2026-09-08-r1-factory-audio-root-plan.md)。

2026-09-11 用户将当前首台调整为[免拆分级授权](docs/2026-09-11-r1-no-disassembly-authorization.md)：`r1-sample01`始终不拆机，授权仅绑定当前3448基线；较低权限路线有可复现实机不足证据后，可按目标分区安全门禁使用必要的root、boot、system/recovery、特权服务和专用Enforcing策略。Loader、分区表、物理首4 MiB、擦除、格式化及整盘覆盖仍须另行授权。电气硬断麦与所有引导组件同时损坏后的恢复改为非强制项，但实体按键强制软件静音、远程不可解除、真实状态/灯效，以及APK/模型/自有用户态组件的签名更新和失败回退仍须交付。R0保持`pending`，本决定不把任何待验证功能写成通过。

2026-09-12 用户停止当前首台及后续需要用户充当测试人员的受控人工验收，包括距离、音量、
方向、说话材料、主观听感、反复操作手机/按键或其他物理步骤。历史真人、固定短语、方向矩阵、主观回听、命中率和交互体验结果只保留为
诊断，不改写为通过。实现直接消费3448原厂四麦、MicArray、DOA、AEC、DSP和校准链输出；
现有材料没有原厂可审计效果阈值时不自行命名“原厂标准”。交付改以可自动复核的功能、数据、
链路、安全、恢复和稳定性为门槛；依赖真人的方向准确率、独立声学响应、唤醒/识别率、音质、
语音保留与插话体验均标记“用户跳过/未验收”，不阻塞后续功能，也不宣称通过。网络/IP
恢复、72小时稳定性、签名回滚和核心功能状态机仍须验证。详见
[现行验证边界](docs/2026-09-12-r1-vendor-native-functional-validation.md)。

### 1.3 首版范围

2026-09-07 当前仅开发和验收首台 R1。下列多台、跨房间和区域组播项保留为后续生产目标，不纳入当前单台门槛。新增需求与现状见[功能盘点](docs/2026-09-07-r1-feature-requirements.md)。

生产版目标：

- 单台和 3～8 台 R1 注册、在线状态及区域归属。
- R1 本地唤醒，唤醒后上传命令音频，HA 侧完成中文 STT、意图、对话和中文 TTS。
- 自然中文对话、多轮对话、回复后自动续听、长答案边生成边播放。
- 自动端点检测、停止词、新唤醒、顶部按键与 TTS 期间自由插话打断。
- 音乐播放中使用唤醒词或停止词打断，命令完成后按策略恢复。
- HA 自动化主动通知、提醒、对话发起和媒体播放。
- 默认在发起指令的设备/区域回复和播放。
- 指定其他房间播放或通知。
- 单设备媒体播放和“区域组播”。
- 网络中断自动恢复、重启自启和诊断。
- 本地闹钟（语音/HA 创建设置、HA 全量显示、R1 执行）、免打扰及必要配置/状态的语音与 HA 双入口管理。
- 手机蓝牙音箱、安卓/iPhone 自助配网（热点网页优先验证，蓝牙配网为条件路线）、按键/静音/灯效及签名更新回滚。
- 内容平台由 HA 接入；支持视频检索、随机/评分筛选并在目标电视播放，具体 HA 接入后续实施，不阻塞 R1 基础功能交付。

不属于生产首版的能力：

- 蓝牙网关（中转其他蓝牙设备数据到 HA）、R1 本地视频解码和独立内容平台。

- 完全复刻原厂波束成形、音色调教及商业音箱级声学仲裁；但 AEC、降噪和播放中唤醒必须达到本文验收指标。
- 多台 R1 亚毫秒级同步。
- 两台 R1 直接组成可靠的左/右声道。
- 同一区域多台设备都开麦时能够像商业音箱一样自动选出最近的一台。

## 2. 需求澄清与能力边界

### 2.1 “直连 Home Assistant”的准确含义

Home Assistant 的实体由 Core 内的集成创建。外部 Android 应用不能仅凭 REST/WebSocket 通用 API 随意注册并持久维护一个新的原生实体。因此“直连并注册实体”有两种合规实现：

- R1 实现 HA 已支持的设备协议，由官方集成创建实体；本方案选 ESPHome Native API。
- 在 HA 内安装自定义集成，由该集成代表 R1 创建实体。

二者都不需要在网络中增加独立网关。STT、TTS 和 LLM 推理服务属于 HA 语音管线后端，不是 R1 接入网关；KWS 则在 R1 APK 内本地运行。

### 2.2 主要能力判断

| 需求 | 交付级别 | 实现方式/验收边界 |
|---|---|---|
| 端侧固定唤醒词 | 硬性 | R1 本地运行 KWS；固定为英文单词 `Alexa`，无前缀；对话管线始终为中文 |
| 自然中文对话 | 硬性 | HA 中文 STT + Conversation Agent；家居意图本地优先，开放问题可路由本地/云端 LLM |
| 多轮对话 | 硬性 | 持久化 `conversation_id`，不丢失上下文且不与其他卫星串话 |
| 持续对话 | 硬性 | 回复播完后默认免唤醒续听 10 秒，最多 5 轮或 2 分钟，HA 可配 |
| 持续/流式回复 | 硬性 | Conversation Agent 输出增量文本，TTS 分句合成，R1 边收边播，不等全文生成 |
| 智能终止 | 硬性 | 本地 VAD + HA STT 端点；支持停止词、新唤醒、按键和 TTS 期间无唤醒插话 |
| 主动播放/推送 | 硬性 | `assist_satellite.announce`、`START_CONVERSATION` 及 `media_player`；支持定向和组播 |
| 播放中唤醒 | 硬性 | 依赖 AEC 和原厂 DSP 路由；音乐中必须说唤醒词/停止词，唤醒后立即 duck 或暂停 |
| 多环境降噪 | 硬性 | 优先原 HAL/Android AudioEffect，其次 WebRTC APM；不叠加互相冲突的处理 |
| 当前房间执行 | 硬性 | 管线传入设备 ID，设备在 HA 分配 Area |
| 在另一设备回复 | 基础定向回复为硬性 | 使用目标卫星 `announce`；无缝迁移多轮会话作为扩展 |
| 同区域一起播放 | 可实现普通组播 | 普通实体组不能保证同步 |
| 严格同步/立体声 | 实验性 | 需要 Snapcast 类时钟同步和声道拆分 |

### 2.3 硬性能力的验收原则

“协议支持”、“API返回可用”或纯主机模拟都不等于已交付。硬件相关能力必须在真实R1上证明原厂链实际运行、数据完整且功能状态正确。第14章中依赖受控真人/房间/指定音量的效果指标按2026-09-12决定标记用户跳过/未验收；安全、恢复、稳定性和可机器验证的功能指标仍是硬门槛。

### 2.4 同一区域多台 R1 的默认策略

首版建议每个区域只启用一台“主监听设备”，同区域其他 R1 可作为媒体播放器和通知终端，但关闭本地 KWS/麦克风监听。原因是多个远场麦克风会同时听到同一句话，HA 标准管线没有商业音箱那种成熟的近场仲裁。

后续若确实需要同一区域多点拾音，再增加仲裁：检测后等待 200～400 ms，各设备交换最近音量/RMS、检测时间和设备优先级，只允许胜出设备继续管线，其他设备取消本次会话。该能力必须用真实户型和摆位验证。

## 3. 已知硬件与软件基础

### 3.1 可参考的公开事实

| 项目 | 已知信息 | 对方案的影响 |
|---|---|---|
| 主控 | Rockchip RK3229 | 适合音频采集和网络传输，不适合在端侧运行重型 STT/LLM |
| 内存/存储 | 约 512 MB RAM、8 GB eMMC | APK 必须轻量，避免大型运行时和模型常驻 |
| 系统 | 社区实机资料指向 Android 5.1/API 22 | 不能依赖现代 Android API；TLS、后台服务和依赖版本需兼容 |
| 麦克风 | 拆解可见四颗麦克风 | 硬件有远场潜力，但阵列算法是否能被第三方调用未知 |
| 音频 DSP | AKM AK7755EN | 原厂音质可能依赖私有固件和路由，不宜轻易替换 Audio HAL |
| 扬声器 | 高低音电子分频 | 单台 R1 更像单声道两分频音箱，不应直接宣传为立体声终端 |
| 网络 | 2.4/5 GHz Wi-Fi、蓝牙 | 8 路 PCM 在正常局域网内带宽可接受 |

硬件数据来自公开拆解，最终以实机 `getprop`、`/proc`、`dumpsys` 和 `/proc/asound` 为准。

### 3.2 社区已证明的可行点

- 已有针对 R1 的 Alexa 替代 APK，包含禁用原语音/播放器包、安装 APK、自启动、录音播放和灯效控制等实践。
- 有社区项目明确记录 R1 为 Android 5.1，并使用精简 ADB、原厂 APK 外壳及 DEX/smali 注入方案。
- 旧资料显示部分 R1 在局域网开放 5555，可直接 `adb connect`；但不能假定每台原版固件都如此。
- 社区 root/官改固件通常需要拆机写入；root 后灯效、私有设备文件和系统包控制更容易。

这些是“存在可行样本”，不是对用户手中设备的保证。

### 3.3 当前真机的应用层来源偏差

2026-09-02 对当前 R1 真机进行只读 APK 来源复核后，确认它不是纯原厂应用层：系统分区仍保留 `Unisound.apk` 和 `EchoService.apk` 等斐讯基础文件，但 Package Manager 当前使用的 `com.phicomm.speaker.device` 与 `com.phicomm.speaker.player` 位于 `/data/app`。两者都是前置 DEX、后置 APK ZIP 的异常双重结构；前者的 DEX 还直接包含第三方 `custom.*` 类和社区修改版本链接，可确认经过第三方修改，后者也高度确定经过第三方修改。

公开 3448 OTA 对照和实机哈希审计已确认系统 EchoService/Unisound、Audio HAL、两套四麦库以及 AK7755 data2 CRAM/PRAM/OFREG 均与 OTA 目标一致，内核也带有斐讯构建标识。因此当前开发基线应定义为：**官方 3448 音频底层文件已哈希确认、第三方应用层覆盖**。这不等于整个 eMMC 已完成逐字节官方镜像验证，但足以确认独立 APK仍在使用 R1 专用内核/HAL/DSP，而非通用 Android 音频驱动。

- 第三方更新包的版本号或后部 v1 原厂签名不能作为代码原厂性的证明。
- 每次录音、播放、AEC 和资源占用测试都记录两个第三方音频包的实际路径、版本和运行/停止状态。
- 第三方包运行时得到的音频独占、路由或增益结果，不直接归因于系统基础 APK 或原厂 Audio HAL。
- 独立 AudioRecord 会使用官方 Audio HAL，但不会自动继承 Unisound 应用通过 `Uni4micHalJNI/libuni4michal` 获得的阵列处理、AEC、DOA 和应用级增益。versionCode 8 实机 PoC 可加载系统 JNI并查询板卡版本，但 `openAudioIn(2)` 被 `untrusted_app` 对 `/dev/snd` 的 SELinux权限拒绝。该结论只淘汰“普通APK直接调用”路线；2026-09-08 起改由专用特权代理、受限SELinux域和必要的Root/系统镜像继续复用私有链路。
- versionCode 9 对 AudioFlinger 提供的 16 kHz 双声道做了同帧拆分；左右语音高度同相，选右声道只同步抬高语音和底噪，平均/差分结果均未发现下混抵消。双声道仅作为诊断输入，生产接口继续固定输出单声道，阶段 1 转向可审计的应用层降噪与动态处理。
- 离线 512 点 STFT 频谱减法已对 gentle/balanced/strong 三档完成同源 A/B；balanced 在活跃窗口、波形相关性和零削波之间暂时最均衡，但整段差仍只有 8.20 dB。必须先通过人工自然度/漏字回听，再按 20 ms 输入接口移植并实测 CPU/PSS；离线处理结果不得直接计为实时音频子门槛通过。
- 首轮回听显示 balanced 能恢复首段可懂度，但原始多段语音本身逐渐变小，固定增益使后续短语不可懂。离线追加 20 ms 活动门控、峰值受限的自适应增益后，候选 C 达到整段 10.43 dB、活跃窗口 11.69 dB且零削波；仍需人工确认三段音量一致性、自然度与漏字，再决定是否实时移植，不能仅凭增益后的门槛数值宣称通过。
- 用户确认该轮后续短语变小源于说话音量过小，因此不再用它判断设备自动衰减。候选 C 的 API 22 Java 移植在 R1 处理 10 秒 WAV 耗时 2.04 秒、线程 CPU 1.92 秒、PSS 约 8.7 MB，输出与 Python 参考逐样本一致；但当前仍整段分配内存，只能作为算法/算力准入。下一实现必须使用有界环形缓冲和增量 STFT 状态，并在真实 AudioRecord 流上验证持续实时率、丢帧和内存稳定性。
- 增量 STFT 已以固定 512 样本窗口/OLA 和 320 样本增益帧接入真实 AudioRecord，2 秒校准缓冲也是固定上限。versionCode 11 将 FFT、解码和输出工作区改为复用，并以不留存音频的独立 Service 完成 20 秒回归和 30 分钟真机 soak：正式轮输入输出各 28800000 样本，部分读取、处理超时和削波均为 0，最长处理块 42.6 ms，实时率约 0.131、线程 CPU 约单核 12.7%；31 个 PSS 样本最大 11.6 MB，首末中位数增长 4 kB，单一 PID 未重启，处理期间无 GC 或分配失败，原服务恢复。该结果通过流式稳定性子项，但不能替代 1 米固定短语的声学子门槛，也不能据此提前进入 KWS。
- versionCode 12 对同一次标准 AudioRecord 采集并行保留原始和实时处理结果。两轮固定短语 A/B 均由用户确认清楚可懂、听感都好且无明显差异；第一轮原始整段/活跃窗口分离为 8.36/12.45 dB，处理后为 5.81/7.13 dB，说明当前固定增益与活动门控把静音噪声抬得比语音更多。第二轮静音基线含单次瞬态，客观对照作废。主观可懂度通过不替代整段 10 dB 门槛，当前处理不选为生产默认；下一步先使静音帧保持衰减，再做同源复测。
- versionCode 13 对非活动帧抵消固定 +18 dB，并将活动阈值收紧到噪声 RMS 的 2.0 倍。同源复测的原始整段/活跃窗口分离为 11.32/14.82 dB，处理后为 29.52/46.50 dB，输入输出完整且无部分读取、处理超时或削波；用户确认处理后 B 清楚且比原始 +18 dB 的 A 更好。结合已确认可闻的测试音和 30 分钟流式稳定性，阶段 1 音频子门槛通过；后续可按第 5.5.2 节进入 KWS 对照，但不得据此宣称 AEC、播放中唤醒或阶段 1 总门槛通过。
- 2026-09-04 按用户要求将 KWS 阶段归零：旧训练数据、真人录音、派生特征、模型、测试证据和模型专用实现已永久删除。阶段 0、标准 AudioRecord、流式音频处理及 API 22 通用兼容结论继续有效。
- 新目标固定为英文单词 `Camila`，发音 `ka-MEE-la /kaˈmila/`，不使用 `Hi`、`Okay`、`Hello` 等前缀；唤醒后的 STT、对话和 TTS 固定为中文。清理范围和不可恢复边界见 [`docs/2026-09-04-camila-kws-reset.md`](./docs/2026-09-04-camila-kws-reset.md)。
- 当前只有一位说话人参与，本轮允许按统一比较结果做单用户最终选型，但跨说话人泛化保持未验证，不能据此宣称项目级 KWS 验收通过。
- 继续使用独立包名开发，不覆盖当前第三方包或系统基础 APK；需要停包时仍执行逐包停止、立即恢复和状态核验。
- 生产目标允许由 HA 卫星替换当前第三方应用层；首次切换仅可恢复地禁用，不直接卸载，替代服务失败时自动恢复。

详细包路径、SHA-256、签名和结构证据记录在 [`docs/2026-09-01-r1-sample01-stage0-report.md`](./docs/2026-09-01-r1-sample01-stage0-report.md)。

### 3.4 为什么不默认刷通用 Linux

RK3229 有通用 Linux/Armbian 内核和电视盒子设备树实践，但“SoC 能启动 Linux”不等于“斐讯 R1 的整机功能可用”。目前没有找到经公开验证、可直接复用的 R1 Linux BSP，至少以下项目仍需板级适配或逆向：

- AK7755 DSP 的上电、固件加载、音频路由和参数。
- 四麦阵列的真实通道数、TDM/I²S 格式、波束成形和录音参数。
- 播放参考信号、全双工 AEC、功放使能、电子分频和音量控制。
- Wi-Fi 固件/校准数据、顶部触摸按键、静音状态和灯效。

2026-09-08 后的推荐顺序固定为：完整备份与回刷验证 → 原厂 Android 上的临时/补丁 Root 与特权音频代理 → 定制 SELinux、boot/recovery/system 或最小原厂服务桥 → 必要时使用原厂内核、DTB、HAL、DSP和校准尝试实验系统。通用 Linux 先从 RAM 或临时介质启动，在 Wi-Fi、麦克风、扬声器、全双工和 AEC 全部达标且已验证回刷前不覆盖 eMMC 主系统。

## 4. 协议选型

### 4.1 协议对比

| 方案 | 原生卫星实体 | 主动通知/对话 | Android 工作量 | HA 维护成本 | 结论 |
|---|---:|---:|---:|---:|---|
| ESPHome Native API 子集 | 是 | 完整度最高 | 高 | 低 | 推荐生产方案 |
| HA 自定义集成 + 自定义协议 | 是 | 可自行实现 | 中 | 高 | 推荐备选 |
| Assist Pipeline WebSocket | 否 | 需另做实体 | 低 | 低 | 推荐真机闭环验证 |
| Wyoming Satellite | 可见但路线已旧 | 新能力不足 | 中 | 中 | 不建议新建 |
| MQTT Discovery | 难以成为完整卫星 | 需大量自定义 | 中 | 中 | 仅适合状态/按钮，不作为音频主链路 |
| MCP | 否 | 不是音频卫星协议 | 不适用 | 不适用 | 只适合给 LLM 暴露工具 |

### 4.2 为什么主方案选 ESPHome Native API

Home Assistant 的 ESPHome 集成已经负责：

- 设备发现与连接。
- Assist Pipeline 的创建和音频流接收。
- `AssistSatelliteEntity` 状态转换。
- TTS、通知、主动对话和计时器事件。
- `media_player` 以及唤醒词、VAD 等配置实体。

当前 ESPHome 语音能力包括设备端唤醒、连续对话、增量意图/流式 TTS、通知、开始对话、计时器和多通道音频等。2026.1 起原 Native API 密码认证已移除，安全连接应使用 Noise PSK 加密。

R1 不是 ESP 芯片，但协议本质是 TCP 上的 Protobuf 消息。已有 Linux Voice Assistant 项目证明非 ESP 设备可以实现 ESPHome API 服务端并被 HA 当作原生卫星。因此 Android APK 可以按同样思路实现最小服务端。

### 4.3 Android 端应实现的最小协议面

开发时固定一个经过测试的 ESPHome/aioesphomeapi 版本，直接从同一提交的 `api.proto` 生成 Java Lite 类，不手写消息编号。最小集合包括：

- Hello、断开、Ping、Device Info。
- List Entities、Subscribe States。
- Media Player 实体与命令。
- Voice Assistant 订阅、开始/停止、音频上传、管线事件。
- Announcement、Start Conversation、配置和计时器事件。
- 外部/端侧唤醒词描述、增量 `intent-progress`、流式 TTS 音频和播放完成回报。
- API feature flags：voice assistant、API audio、speaker/media player、announce、start conversation、timers。
- Noise PSK 握手与帧加密。
- mDNS `_esphomelib._tcp` 发布，默认端口 6053。

首版只实现 API 音频，不实现 ESPHome 的所有传感器和服务。对未知新消息安全忽略；API 次版本不兼容时在日志中明确报错。

### 4.4 真机闭环验证使用 Assist Pipeline WebSocket

在写 ESPHome 协议前，建议先用 HA `/api/websocket` 验证音频链路：

1. 认证后调用 `assist_pipeline/run`。
2. `start_stage` 设为 `stt`：唤醒已在 R1 端完成，同时传入 `wake_word_phrase`、`device_id`、中文管线 ID 和已有 `conversation_id`。
3. 收到 `stt-start` 及 `stt_binary_handler_id`。
4. 二进制帧首字节放 handler ID，后接 PCM。
5. 发送仅含 handler ID 的帧表示音频结束。
6. 读取 STT、Intent 和 TTS 事件，下载 TTS URL 并播放。

2026-09-05 版本 38 偏差修正：实机识别为 `alexa现在几点？` 时 HA 将 `alexa现在` 匹配为设备名。`wake_word_phrase` 不清理识别文本，因此开发路径拆分为 STT→STT 与 intent→tts 两次请求；客户端仅在内存中移除句首 Alexa 和紧随分隔符。保留命令预缓冲，第二次请求延续已知 conversation_id。主机回归通过；用户后续提供 HassGetCurrentTime 正确返回中文时间的记录，单轮问时意图通过。本轮时间回复可闻性尚未明确确认，正式成功率及稳定性验收保持未验证。

这一步通过后，说明 R1 音频采集、端侧唤醒、网络和 HA 中文管线成立；之后再实现原生实体协议，能显著缩小调试范围。

## 5. Android APK 设计

### 5.1 兼容性原则

- `minSdk` 按 API 22；是否能提高以实机为准。
- 尽量使用 Java/Kotlin 标准库和 Protobuf Lite，控制 DEX、内存和方法数。
- 使用 Android 5.1 兼容的前台 Service；无屏运行不依赖 Activity 常驻。
- APK 使用独立包名，真机开发阶段不覆盖原厂包。
- 不把 HA 长期令牌或管理员凭据编译进 APK。
- 所有现代 API 都必须有 API 22 兼容分支。

### 5.2 模块划分

```text
R1SatelliteService
├─ DeviceIdentity        稳定 ID、配对密钥、版本
├─ EsphomeApiServer      TCP、Protobuf、Noise、mDNS
├─ AudioCapture          AudioRecord、重采样、环形缓冲
├─ AudioProcessing       AEC/NS/AGC 能力探测与开关
├─ WakeWordEngine        sherpa-onnx/TFLite KWS、词表、阈值
├─ EndpointController    本地 VAD、端点、停止词和插话
├─ PipelineController    会话 ID、续听窗口、超时、取消
├─ AudioPlayback         流式 TTS、通知、媒体、Audio Focus
├─ HardwareAdapter       顶部按键、静音、灯效
├─ BootReceiver          开机启动
└─ Diagnostics           日志、指标、本地状态页
```

### 5.3 配置与配网体系

本节描述目标配置面，不代表当前首台 R1 真机已经全部实现。当前已有原生协议、部分配置及运行基础，逐项状态见[功能盘点](docs/2026-09-07-r1-feature-requirements.md)，未完成的能力仍须实机验收。

配置采用“**Home Assistant 主控、R1 本地/ADB 恢复**”的两级结构：日常参数支持语音和 HA 设备页/配置实体双入口管理，首次联网、设备身份、Noise PSK 与失联恢复留在设备侧。生产模式由 HA 的 ESPHome 集成主动连接 R1，R1 不保存 HA 地址、管理员凭据或长期令牌。阶段 2 的 Assist WebSocket 地址和临时认证只用于真机闭环开发验证，不进入生产配置。

#### 5.3.1 参数、设置位置与阶段

| 参数 | 设置位置与方法 | 类型 | 交付阶段 |
|---|---|---|---|
| Wi-Fi SSID、密码 | 当前沿用原系统/授权 ADB；新增安卓/iPhone 自助配网目标，热点网页优先验证，不满足时验证蓝牙方案 | 设备基础配置 | 单台待实现/验收 |
| IP 地址 | 默认使用 DHCP；需要固定地址时在路由器设置 DHCP 保留，不把 IP 作为设备 ID | 设备基础配置 | 阶段 3 |
| 不可变设备 ID | 首次初始化生成并持久化；不直接暴露 MAC、Android 序列号等硬件标识 | 设备身份 | 阶段 3 |
| ESPHome 节点名 | 首次配对时通过本地/ADB 初始化工具设置，例如 `r1-living-room-a`；每台必须唯一 | 设备身份 | 阶段 3 |
| 友好名称、实体名称、Area | 通过语音或 HA 修改友好名称及 Area，以 HA 注册表为准；Area 提供默认上下文，内部 ID 不变 | HA 标准配置 | 阶段 3、5 |
| Noise PSK | 每台生成独立的 32 字节 Base64 密钥；在 R1 初始化和 HA ESPHome 配对时分别写入，轮换时双方重新配置 | 安全配置 | 阶段 3 |
| Native API 端口 | 固定为 6053；首版不开放用户修改 | 固定协议参数 | 阶段 3 |
| Assist Pipeline | 在 R1 的 HA 配置实体中选择；管线内显式使用中文 STT、Conversation 和 TTS | HA 卫星标准配置 | 阶段 3 |
| 当前唤醒词 | 当前固定现有 Alexa 预训练模型，最多激活一个；可查询状态，不因双入口需求重启模型选型 | HA 卫星标准配置 | 阶段 3、4 |
| 本地监听/使用唤醒词 | HA 开关可以停用 KWS；物理静音状态始终具有更高优先级 | 标准能力与 R1 硬件扩展 | 阶段 4 |
| VAD 灵敏度 | HA 只提供低、标准、高等经过实机标定的档位，不直接暴露未经约束的底层值 | HA 卫星标准配置 | 阶段 3、4 |
| KWS threshold、boost | 放在 HA 高级配置区，按当前模型限制有效范围并提供恢复推荐值 | R1 扩展配置 | 阶段 4 |
| 扬声器音量、播放控制 | 通过 `media_player` 设置音量、播放、暂停、停止、上一首和下一首 | HA 标准实体 | 阶段 3 |
| 通知插播策略 | HA 下拉项选择 duck、暂停后恢复或覆盖播放 | R1 扩展配置 | 阶段 3、4 |
| 连续对话/回复后续听 | HA 开关控制是否续听；超时只提供经过验收的固定档位 | R1 扩展配置 | 阶段 4 |
| 唤醒和结束提示音 | HA 开关分别控制；提示音不能代替真实的监听/静音状态指示 | R1 扩展配置 | 阶段 4 |
| AEC/NS/AGC | 高级配置只提供实机验证过的处理档案，例如原厂、Android Effect 或 WebRTC；禁止叠加冲突链路 | 工程/声学配置 | 阶段 4 |
| 麦克风 `AudioSource` | 仅在真机诊断/ADB 调试中选择；生产版锁定通过验收的输入源 | 工程参数 | 阶段 1 |
| 音频格式 | 固定 PCM S16LE、16 kHz、单声道、20 ms/帧，不开放用户修改 | 项目硬约束 | 全阶段 |
| 日志级别 | HA 高级配置或本地诊断入口设置；默认 `INFO`，临时 `DEBUG` 到期自动恢复 | 运维扩展 | 阶段 3、4 |
| 诊断音频采集 | 默认关闭，只能显式、限时开启并记录来源、时间和用途 | 隐私扩展 | 阶段 4 |
| APK/模型更新 | 只接受版本、SHA-256 和签名都通过验证的包，失败回滚；不接受任意动态库 URL | 运维扩展 | P1 |
| 重启、设备识别 | 语音与 HA 调用已支持的服务重启和灯效/提示音识别；整机重启须独立验证权限及结果 | 常见设备能力 | 阶段 3、4 |
| 网络恢复、重新配对 | 无 HA/原 Wi-Fi 时也应能通过本地入口配网；原厂入口/授权 ADB 为维护兜底，按键及手机流程须实机验证 | 恢复能力 | 阶段 3、4 |

只读诊断状态与可写配置必须分开。设备可以上报 IP、RSSI、固件/APK 版本、运行时间、HA 连接状态、静音状态、当前模型、CPU/PSS、温度、音频丢帧和最近错误，但不得返回 Wi-Fi 密码、Noise PSK、HA 令牌或录音内容。

#### 5.3.2 首次设置和日常修改流程

1. 当前使用原厂入口或授权 ADB 接入 Wi-Fi；目标为通过已验证的安卓/iPhone 本地配网入口设置，默认 DHCP，需要固定地址时在路由器保留租约。
2. 安装独立包名 APK，通过一次性本地初始化流程生成设备 ID、唯一节点名和每台独立的 Noise PSK。密钥不得出现在命令行回显、仓库或日志中。
3. R1 通过 mDNS 发布 `_esphomelib._tcp`。在 HA 的“设置 → 设备与服务”接受 ESPHome 发现结果并输入该设备的 PSK；发现失败时手工填写主机名/IP 和端口 6053。
4. 通过语音或 HA 管理友好名称、Area、音量、语速、等待时间、闹钟、免打扰等已支持配置并回读结果。中文 Pipeline 与当前 Alexa 保持项目约束；threshold、boost、AEC/NS/AGC、日志和诊断录音归入受限高级配置，不因双入口需求重新开展模型或声学选型。
5. Wi-Fi 失联时目标为本地手机恢复，热点网页与 BLE 均需 API 22 实机验证，不能写成已支持。原厂入口或授权 ADB 继续作为维护兜底，无线 ADB 不保证完全失联时可达；恢复原包必须先释放卫星音频。

#### 5.3.3 持久化、安全和优先级

- Wi-Fi 凭据继续由 Android 系统保存，卫星 APK 不复制或导出；Noise PSK 存入应用私有目录，并使用 Android Keystore 包装的本地密钥保护。API 22 上密钥是否由硬件保护必须在 R1 实机检查，不能仅凭 Keystore API 可用就宣称硬件级安全。
- 普通配置使用带版本号的持久化记录。HA 下发值验证成功后原子保存，APK 重启、HA 重启和断网重连后保持；非法值或未知枚举必须拒绝并保留旧值。
- 配置优先级固定为：物理静音与安全限制 → 本地身份/安全配置 → HA 持久配置 → APK 默认值。任何远程操作都不能绕过物理静音或自动关闭认证。
- 配置变更必须按设备隔离并记录时间、来源和结果。修改一台 R1 的唤醒词、音量、Area 或 Pipeline 不得影响其他设备。
- 顶部按键恢复模式和签名更新须通过首台实机恢复/回滚验证；批量配置另待 3～8 台隔离测试，不以多台检查阻塞当前单台交付。

#### 5.3.4 必要配置与状态的语音/HA 双入口

必要的可写配置支持语音查询/修改和 HA 查看/修改，只读状态支持语音查询和 HA 展示。包括音量、语速、等待时间、闹钟/计时器、免打扰、友好名称/Area、灯光/提示音、媒体/蓝牙控制及已支持的系统管理操作；网络、版本、故障和物理静音等只读状态如实展示。完整入口及验收状态见[功能盘点](docs/2026-09-07-r1-feature-requirements.md)。

两个入口共用配置和状态来源。R1 配置以设备保存确认及回读为准，HA 名称/Area 以注册表为准；并发操作后状态须收敛。不得把失败、设备离线、待同步或过期状态显示为已生效。配置重启持久化，设备身份不因改名或改网变化。只读状态不可误写；不朗读或展示 Wi-Fi 密码、Noise PSK 等凭据，不开放所有底层工程参数。语音管理使用 HA 中文链路，首次配网和失联恢复走本地入口，物理静音不可被远程绕过。

新增 HA 闹钟管理及同步状态、免打扰、系统管理入口，并扩展现有媒体控制。沿用固定版本生成协议；标准协议未覆盖的需求另行设计受认证扩展，不手写消息编号，不把目标接口写成已支持。

#### 5.3.5 本地闹钟、计时器与免打扰

闹钟支持语音与 HA 创建、查询、修改、取消；全部已有闹钟（包括语音创建的）必须在 HA 显示，提供名称、时间、重复日期、启停、铃声、音量、下次触发和同步状态。R1 本地保存已确认配置与所需铃声并调度响铃，支持停止、稍后提醒及回报；HA/网络离线不影响已同步闹钟执行。HA 保留最后同步列表并标记离线，未确认修改显示待同步，重连后回读并避免重复触发或已取消记录复活。

闹钟采用日历时间，计时器采用倒计时；验证重启、时区、校时、时间跳变及错过触发处理。断电期间无法响铃，离线冷启动时间可信度须实机验证，不承诺无有效时钟时准确响铃。本地执行不代表新增断网语音识别。

2026-09-12 v104 已在主机侧实现固定 ESPHome 计时器事件、本地私有持久化、暂停/恢复、运行时单调时钟、重启可信墙钟、不可信时钟待定状态、断网本地合成铃声、停止和重复 finished 去重，并在行为齐备后声明 `TIMERS`。该实现尚未部署首台，真实协议、API 22 存储、重启、铃声与抢占保持待验证；完整日历闹钟、稍后提醒及 HA 全量管理仍是后续独立交付。

同日 v106 已完成本地日历闹钟内核的主机实现：单次日期与每周重复、本地时区重算、不可信时钟门禁、5分钟错过窗口、停止/稍后提醒、触发去重、版本化私有持久化、并发冲突和写入失败回滚。它与计时器共享合成铃声但分别持有所有权。HA 全量展示/认证同步、中文语音管理、按键产品映射及首台实机验证仍未完成，故本增量不等于完整闹钟交付。

免打扰支持手动和时段配置、本地执行，默认屏蔽普通主动播报并降低灯光，不补播过期通知；保留主动唤醒及回复，闹钟默认允许响铃且例外可配置。免打扰不是麦克风静音，必要隐私指示保持真实。

#### 5.3.6 手机配网与蓝牙音箱

安卓/iPhone 自助配网是新增目标。优先验证 R1 热点＋手机网页；不满足独立 APK、两类手机、无 HA/原 Wi-Fi 可进入、安全传输、失败可恢复条件时，验证并采用 BLE 配网。两者均不可行则记录阻塞，不自动扩大为 root 或刷机。API 22 权限、热点与 BLE 能力及来源说明见[路线对比](docs/2026-09-07-r1-feature-requirements.md)。

手机蓝牙音箱独立验证固件接收能力、原包隔离后的音频路径、配对/断开及音频焦点。蓝牙网关用于中转其他蓝牙设备的数据至 HA，当前不增加，与手机音箱及配网无关。

### 5.4 音频采集

首轮依次测试 Android `AudioSource`：

1. `VOICE_COMMUNICATION`：系统若支持，可能自动使用 AEC/AGC。
2. `VOICE_RECOGNITION`：通常更适合语音识别，Android 文档说明它不会主动套用 AGC/NS。
3. `MIC`：作为兼容兜底。

每个输入源分别保存 10 秒测试 WAV，比较静音底噪、说话幅度、播放回声和是否只有一个麦克风通道。不能只凭 API 返回“可用”判断效果。

生产管线格式固定为：

- PCM S16LE。
- 16 kHz。
- 单声道。
- 20 ms/帧，即 320 个采样、640 字节。
- 带序号和单调时钟时间戳。
- 保留 1.5～2 秒环形预缓冲，端侧唤醒成功后先上传预缓冲，避免截掉命令开头。

空闲期只保持加密控制连接，不向 HA 持续上传环境音。唤醒后单台 PCM 约 256 kbit/s；即使 8 台同时上传也约 2.048 Mbit/s，普通局域网足够。PCM 是 ESPHome API 音频的首选；Opus 仅在后续自定义协议或弱网需求下评估，不为节省局域网带宽增加首版复杂度。

### 5.5 R1 端侧唤醒与本地 VAD

R1 常驻 APK 在空闲状态只运行轻量音频前处理、VAD 和 KWS，不在终端运行完整中文 STT。目标唤醒词按 2026-09-05 用户要求改为英文单词 `Alexa`，不增加前缀，使用现有预训练模型。唤醒后的 STT、意图/对话和 TTS 全部固定为中文，不做自动语言检测。

#### 5.5.1 候选范围与统一接口

当前采用 ESPHome 官方 Alexa microWakeWord v2 预训练模型，固定版本与哈希，停止 Camila 自训练。必须验证 Android 5.1/API 22 和 armeabi-v7a 兼容性；许可与接入证据见 [Alexa 接入记录](./docs/2026-09-05-alexa-pretrained.md)。

Android 端通过模型无关的 `KwsEngine` 接口运行候选：每次只加载一个引擎，输入固定为 PCM S16LE、16 kHz、单声道、20 ms/帧。接口统一提供初始化、状态重置、逐帧输入、检测事件、关闭和性能统计。模型不得持有 AudioRecord，也不得默认保留 PCM。

版本 32 已接入 Alexa 滑窗事件及限时前台监听，判定遵循 ESPHome 2026.8.0 的原始阈值 229、窗口 5 和低分特征冷却 100 帧。外部 PCM 格式不变；本轮基线直接将 VOICE_COMMUNICATION 输出送入 microfrontend，暂不叠加自研降噪/增益。临时隔离音频修改包后 R1 20 秒运行及停止/重启检查通过，共存路径仍录音阻塞。真人效果、常驻稳定性及生产音频共存未验收，证据与偏差见 [Alexa 监听记录](./docs/2026-09-05-alexa-listener.md)。

#### 5.5.2 数据隔离与比较方法

Alexa 使用预训练权重，不要求重新采集训练集。采集至少 20 个独立验证样本、模型/阈值冻结后的至少 30 个盲测样本，以及至少 30 条 Alexa 近音或非唤醒短语；按完整会话隔离，盲测不得用于调参。旧 Camila 正样本与近音结果不得替代 Alexa 验收。

统一报告命中数、召回率、近音误触发、环境误唤醒次数/FAPH、词尾到检测的 P50/P95、初始化耗时、模型/APK 大小、PSS、CPU、实时率和最大单帧耗时，并单列框架、模型、词表和训练数据许可。技术门槛仍按第 14 章显示通过/失败，但不自动代替用户选型。

当前单人结果只能标记为“单用户定型”。用于多人或 3～8 台部署前，必须补足至少三位说话人的独立盲测；补测前跨说话人验收状态为“未验证”。

截至 2026-09-05，microWakeWord v2 checkpoint 3500 INT8 已冻结为盲测候选，阈值为原始输出 `255/255`。单人 validation 为 34/34，真人近音为 1/31（Samira），公共环境音为 1 次/9.670475 小时；R1/API 22 的实际 microfrontend 和流式 TFLite 回放结果一致。该结果达到第 14 章召回与环境误唤醒门槛，但盲测、跨说话人、远场、播放中唤醒和 24 小时稳定性尚未验证，不能标记为 KWS 已交付。

同日完成冻结后的三轮独立盲测，共 45 条，首次揭盲为 35/45（77.78%）；R1 逐条复核同为 35/45，主机与 R1 分数零差异。该模型低于 95% 唤醒成功率门槛，正式判定失败，不作为最终模型。若将这 45 条用于后续 v3 开发，必须重新采集未参与调参的独立盲测集。

本地 VAD 用于减少无效计算、向 HA 提供端点候选，并在 TTS 期间检测用户插话。最终命令结束由本地 VAD 与 HA STT VAD 共同判定，避免短暂停顿过早截断中文语句。

### 5.6 AEC、降噪和播放中唤醒

Android 的 `AcousticEchoCanceler`、`NoiseSuppressor` 和 `AutomaticGainControl` 只能说明框架有对应接口，仍要检查 `isAvailable()`、创建结果、启用结果和实测波形。

测试组合：

1. 原厂服务运行 + 第三方录音。
2. 停止当前第三方播放器/语音更新包 + 独立 APK 录音。
3. `VOICE_COMMUNICATION`。
4. `VOICE_RECOGNITION`。
5. Android AEC/NS/AGC 开关组合。
6. WebRTC APM，但只在系统处理不足时启用。

若无法获得扬声器参考信号，软件 AEC 很难可靠消除 R1 自己播放的音乐。生产策略是：

- TTS 期间持续运行受 AEC 保护的 VAD；确认是用户人声后立即停止 TTS，无需再说唤醒词。
- 音乐播放时不因普通聊天停止；本地 KWS 或停止词命中后在 300 ms 内 duck/暂停。
- 命令结束后按原媒体状态和用户指令决定是否恢复，不盲目自动播放。
- 播放中唤醒和TTS插话必须完成采集持续、事件触发、停止/取消、迟到数据隔离和恢复等机器可验证功能；真人命中率、听感和语音保留效果按2026-09-12决定不作用户验收。

### 5.7 播放、流式回复与打断

2026-09-07 优化纳入（待实现/验证）：按[O2/O3/O8](docs/2026-09-07-r1-chain-optimization.md)统一 TTS、媒体、蓝牙、播报、闹钟的播放所有权、停止和恢复。按键、新唤醒、直接插话分别交付；本地停声后向 HA/TTS/模型传播取消，隔离旧任务迟到音频和事件。后端不支持取消时报告限制，不把本地丢弃等同于后台任务停止；停止回答不撤销已执行动作，用户明确停止的媒体不自动恢复。真流式须证明首段播放早于全文生成，工具结果确认前不得播报已完成。转换直通、缓存及缓冲调整以测量为依据，不以容量推断启动延迟。

- 音乐使用长期 `AUDIOFOCUS_GAIN`。
- TTS/通知使用兼容 API 22 的 transient focus。
- 新唤醒、顶部停止按键或 HA stop 事件立即停止当前 TTS。
- 长答案使用 HA `intent-progress`/增量文本和支持流式的 TTS；R1 收到首个可播音频块就开始播放，不等全文。
- TTS 期间的自由插话会取消旧管线和剩余音频，并在同一 `conversation_id` 下开始新一轮。
- 通知可选择 duck 音乐或暂停音乐；语音类内容优先暂停。
- 播放真正结束后才回报完成，否则 HA 的卫星实体会卡在 `responding`。
- 每次异常退出都释放 `AudioTrack`/`MediaPlayer`、Audio Focus 和 WakeLock。

### 5.8 状态机

2026-09-07 O4/O5 补充：续听窗口、对话记忆及待确认操作分别管理生命周期；协议 ID 保留不等于模型记忆有效，需核验实际下游期限与取消语义。配置区分请求、应用结果和实际状态，旧缓存同值不作为本次完成证据；标准实体核验连接和状态新鲜度，自定义同步关联请求及配置版本。并发、重复、离线与待同步如实反馈，原权限及来源隔离保留。

```text
BOOTING → DISCONNECTED → CONNECTING → LOCAL_WAKE_LISTENING
                                               ↓ wake/button
LISTENING → PROCESSING → STREAMING_RESPONSE → FOLLOW_UP_LISTENING
    ↑              ↘ cancel/error ↗             ↓ timeout/max-turn
    └──────── barge-in/new wake ───────────────┘
                                               ↓
                                      LOCAL_WAKE_LISTENING
```

并行状态包括 `MUTED`、`PLAYING_MEDIA`、`ANNOUNCING`、`UPDATING`。任何新管线启动前先取消旧管线，同一设备只允许一个活跃会话。

### 5.9 按键、静音与灯光的交付边界

完成音量、停止、静音、闹钟停止/稍后提醒及本地配网入口的按键适配；支持的功能可通过语音查询并在 HA 查看。灯光表示监听、处理、播放、静音、失联和配网，亮度及提示音等允许配置的项目支持双入口管理。静音必须由实体按键触发特权层停止采集，语音和HA不能解除，状态与灯效真实且重启后保持。只有实机验证切断麦克风供电或信号链后才能另称“硬件断麦”；电气硬断麦不是当前强制项，关闭监听后不承诺用语音重新打开麦克风。

## 6. Home Assistant 端部署

2026-09-07 O3/O5/O6 纳入：当前 R1 会话适配仍等待完整结果，音频分块不能替代端到端增量回答。后续在家庭模型中心、路由、R1 适配和 TTS 间交接增量、取消、工具结果和会话期限契约，记录两侧受测版本；外部私有实现不复制到公开仓库。固定 HA 私有接口依赖集中维护，加载检查、不兼容明确报告，事件更新优先、轮询补偿，卸载恢复被替换行为。此安排不修改 HA Core、不切换现有后端或放宽设备权限。

### 6.1 HA OS 基础语音栈

在 HA OS 中安装并配置：

1. **中文 STT**：优先在 HA/PVE 使用 SenseVoice/FunASR 类中文优化后端；若使用 Whisper App，对 `zh` 使用官方推荐的 `stt_library: auto`。显式设置 `language: zh`，不用 `auto` 语言检测。
2. **中文 TTS**：本地基线可用 Piper `zh_CN-huayan-medium`、`zh_CN-chaowen-medium` 或 `zh_CN-xiao_ya-medium`。硬性的长回复需求要求所选 TTS 真正支持边合成边输出；不支持时必须换用兼容的本地或云端中文 TTS。
3. **Wyoming 集成**：连接 STT/TTS 后端。HA 唤醒服务仅作调试兜底，生产唤醒在 R1 本地。
4. **Conversation Agent**：家居确定性指令优先使用 HA 本地意图；自然对话、知识问答和复杂规划优先本地 LLM，本地性能不足或服务异常时才路由云端中文模型。
5. **Assist Pipeline**：STT、Conversation 和 TTS 语言全部显式设为中文；保留 `conversation_id`、`device_id` 和 Area 上下文。
6. **资源和工具**：由 HA 向对话代理提供经授权的实体、自建资源库、本地文件、网址检索和云端 AI 能力。高风险设备和操作要最小权限并支持语音二次确认。

Whisper 官方文档仍给出“树莓派 4 约 8 秒、Intel NUC 低于 1 秒”的量级差异；当前 App 已增加 SenseVoice/FunASR 对中文的自动选择，因此必须在用户的 PVE 主机上以实际命令测试，而不是只按旧 Whisper 经验采购。

### 6.2 PVE/HA OS 资源建议

先分配以下首台真机开发基线；此表只覆盖 STT/TTS 和 HA，不包含大型本地 LLM 的 GPU 需求：

| 资源 | 首台真机开发基线 | 3～8 台推荐起点 |
|---|---:|---:|
| vCPU | 4 | 6～8 |
| RAM | 8 GB | 12～16 GB |
| HA OS 磁盘 | 32 GB | 64 GB |
| 网络 | 1 Gbit 虚拟网卡 | 同左，桥接至 R1 所在 LAN/VLAN |

物理 CPU 以 Intel N100 或更高的单核/多核性能作为本地开放式 STT 参考线。这里的 6～8 vCPU 不是保证值：PVE 的 vCPU 来自物理主机，共享超分会显著影响延迟。

扩容判据：

- 8 路并发命令/STT 时 HA VM 持续 CPU 超过 75%。
- 语音结束至 STT 完成的 P95 超过 1.5 秒。
- HA Core 因推理出现事件循环延迟或自动化延迟。
- 本地 LLM 与 STT 同时运行发生内存交换。

若触发任一项，把 STT/TTS/LLM 放到 PVE 内独立推理 VM/容器，并通过 Wyoming 或对应 HA 集成接入；这仍属于 HA 语音后端，不是 R1 网关。KWS 仍在 R1 本地。需要本地大模型时单独评估 GPU 和显存，不让 LLM 与 HA Core 抢占同一内存配额。

### 6.3 端侧唤醒词的远程配置

生产 KWS 不包装成 HA 端 Wyoming wake-word 服务，而是直接编入 R1 APK。APK 通过 ESPHome 协议向 HA 公布可用唤醒词，并接受激活模型、threshold、boost、麦克风静音和本地监听开关。模型更新必须有版本、SHA-256、签名验证和回滚位，不允许 HA 下发未验证的任意动态库。

可先使用成熟的英文短语降低 KWS 调优成本；这不影响后续全中文对话。无论英文还是中文唤醒词，都建议选 3～5 音节且不常出现在日常对话中的短语，再用下列数据验收：

- 3 名以上家庭成员，每人至少 30 次正样本。
- 1 m、3 m、5 m 和不同方向。
- 电视、音乐、厨房、人声背景各至少 30 分钟负样本。
- 相似读音、只说前半段、只说后半段等 hard negatives。
- 初始 threshold/boost 通过离线集确定，再用真实房间调优。

目标不是追求一个固定阈值，而是达到：正常环境单次唤醒成功率 ≥95%，8 小时家庭背景声误唤醒不超过 1 次；若做不到，换唤醒短语而不是无限降低阈值。

## 7. 多设备、区域和回复路由

### 7.1 设备建模

每台 R1 使用不可变设备 ID，并在 HA 设备注册表中分配区域。建议命名：

- `r1-master-bedroom-a`
- `r1-living-room-a`
- `r1-study-a`

不要把 IP 地址作为唯一 ID；IP 只作为可变化的连接地址。

### 7.2 默认路由规则

- 管线由哪台 R1 发起，普通 TTS 就回哪台。
- 未说房间名的家居指令使用发起设备所属 Area 作为上下文。
- 说出房间名时，以显式 Area 覆盖默认 Area。
- “在客厅播歌”目标是客厅 `media_player`。
- “全屋通知”才向通知组广播，不默认所有设备一起回复。
- 跨设备回复使用目标设备的 `assist_satellite.announce`。

“在主卧提问、让书房接着进行同一个多轮会话”不是标准默认能力。首版可以把结果通知到书房；真正的会话迁移需要保存 `conversation_id`、授权目标设备接管并处理两边竞争，列为后续功能。

### 7.3 主动推送示例

```yaml
action: assist_satellite.announce
target:
  entity_id: assist_satellite.r1_master_bedroom_a
data:
  message: "洗衣机已经完成。"
```

实体名称以实际注册结果为准。若设备在播音乐，通知策略由 APK 配置为 duck、暂停后恢复或覆盖播放。APK 必须实现原生 `announce`；如果所固定 HA/ESPHome 版本的原生 announce 音频流存在提前结束问题，HA 自动化使用 `tts.speak` 经目标 `media_player` 播放作为可靠回退。无论哪条路径，都必须在音频真正播完后才回报完成。

## 8. 音乐与多房间音频

### 8.1 首版分区播放

每台 R1 暴露 `media_player`，实现 URL 播放、暂停、停止、音量、上一首和下一首。HA 的普通媒体组可以统一下发命令，但各 Android 播放器的网络缓冲和调度不同，所以它只是“组控”，不是严格同步。

自建资源库或 Music Assistant 可作为媒体来源。商业音乐平台是否可语音搜索、能否播放完整曲目取决于账号、接口和版权，不属于 R1 卫星协议本身。

### 8.1.1 HA 内容服务、电视播放与音频协调

天气、资讯、音乐、有声小说等由 HA 对接自建或第三方平台，R1 负责交互和音频播放，不维护独立内容账号或视频解码。视频支持指定名称、搜索、随机和按评分筛选后在目标电视播放：语音→HA 理解/检索→确定内容与电视→播放→反馈。候选或目标不明确时询问，无结果、电视离线及播放失败时明确反馈。具体平台接入后续在 HA 实施，不阻塞 R1 基础能力交付，也不据此宣称内容服务已完成。

统一协调 TTS、媒体、蓝牙、通知和闹钟。闹钟抢占普通媒体，免打扰过滤普通通知，停止终止当前声音；按中断原因和策略恢复媒体，用户明确停止的内容不自动恢复。播放控制与真实状态回报支持语音/HA 双入口。

### 8.2 严格同步

需要严格同步时引入 Snapcast：

```text
音乐源/Music Assistant
        → Snapserver（统一时间轴）
        → 各 R1 内 Snapclient
        → Android 低层音频输出
```

Snapcast 会给音频块加服务端时间戳，客户端持续校时并按目标时刻播放，官方项目称典型偏差可低于 0.2 ms；实际 R1 还受 Android 5.1、DSP 和输出缓冲影响，必须重新测量。Snapcast 支持 Android 的 OpenSL ES/Oboe 路线，但要验证现有 Android 客户端或自行编译的 `snapclient` 是否兼容 API 22。

### 8.3 左右声道

两台 R1 组成左右声道还需要：

- 在服务端把立体声拆成左、右单声道流。
- 固定设备角色并禁止自动交换。
- 分别校准扬声器/DSP 固定延迟。
- 验证单台 R1 内部电子分频没有重新混合或改变相位。
- 运行 60 分钟后仍保持可接受的声像。

这属于独立音频工程，不能通过 HA `group` 自动得到。建议首版只做区域组播；若严格同步通过，再评估左右声道。

## 9. R1 采购指标和销售检查单

### 9.1 一台合格 R1 的关键指标

| 项目 | 目标/已知规格 | 通俗解释 | 采购时怎么判断 |
|---|---|---|---|
| 设备 | 斐讯 PHICOMM R1 智能音箱 | 不是斐讯 N1 盒子，也不是其他品牌的 R1 | 要求发整机、底部标签和开机视频 |
| 主控 | Rockchip RK3229 | 足够录音、本地唤醒和传音频，不适合在音箱上跑大模型 | 到货用 `getprop`/`/proc/cpuinfo` 复核 |
| 内存/存储 | 512 MB RAM / 8 GB eMMC | 只适合轻量 APK 和小 KWS 模型 | 到货检查系统不频繁重启、存储无 I/O 错误 |
| 系统 | Android 5.1，预期 API 22 | 系统很老，APK 必须专门做兼容 | 销售最好回传 `getprop ro.build.version.sdk`，应为 `22` |
| CPU ABI | 预期 `armeabi-v7a`/32 位 ARM | 决定 APK 里的本地音频库能否运行 | 回传 `getprop ro.product.cpu.abi` |
| 麦克风 | 公开拆解为 4 麦阵列 | 决定远场唤醒上限，进水/维修可能使部分麦失效 | 销售演示原唤醒（如服务仍可用）或录音；到货保存测试 WAV 复核 |
| 音频 DSP | AKM AK7755 | 原厂降噪、回声处理和音质可能依赖它 | 不买“刷机后麦克风/音效异常”的机器 |
| 扬声器 | 高低音两分频，公开参数 15 W | 一台是单体音箱，不是左右立体声 | 要求演示 30%、60%、90% 音量，不得破音、杂音 |
| 网络 | 2.4/5 GHz Wi-Fi | 可在局域网直连 HA | 要求演示配网和连续播放，不频繁断线 |
| ADB 5555 | **必须可连，或销售能提供可验证的拆机开启方案** | 这是把新 APK 装进音箱的调试通道 | 优先要求销售回传 `adb connect IP:5555` 成功截图 |
| APK 安装 | **必须可安装/覆盖测试 APK** | 只能连 ADB 但不能装程序，仍然不合格 | 到货在退货期内执行探针 APK 安装 |
| Android root | 最好可通过 `su` 或 root ADB 验证 | 用于停用冲突服务、配置系统自启和读取受限资料；不等于 ADB | 回传 `adb shell su -c id` 或 `adb shell id` 的 `uid=0(root)` 输出 |
| 物理状态 | 按键、麦克风、扬声器、Wi-Fi、电源全部正常 | 任一个坏件都可能破坏硬性功能 | 要开机视频，询问是否进水、拆修、摔落或长期异常发热 |

公开硬件参数只用于筛选型号，不能代替实机验收。真正决定是否采购的是 ADB/APK 入口、麦克风链路、AEC 和 24 小时稳定性。

### 9.2 可直接发给销售的话

> 我要买斐讯 R1 音箱做二次开发，请帮我确认：\
> 1. 能否正常开机、连 Wi-Fi、播放声音，麦克风和顶部按键是否正常？\
> 2. 机器是否进水、拆修、刷过机或 root？如果刷过，请告知固件名称和版本。\
> 3. 局域网 ADB 5555 是否打开？能否用 `adb connect 音箱IP:5555` 连接？\
> 4. 能否通过 ADB 安装或覆盖 APK？\
> 5. 是否保留原厂 Android 5.1 并已 root？请回传 `adb shell su -c id`；不接受只看到 root 管理应用图标。\
> 6. 请尽量发开机、播放、ADB 连接和 root 验证的连续视频/截图。\
> 7. 如果没有 ADB，你们是否支持拆机备份原固件、开启 ADB/root，并保证麦克风、音效、按键和 Wi-Fi 仍正常？能否提供回刷文件？\
> 8. 不要刷通用 RK3229/电视盒子 Linux 固件，不替换 Audio HAL、DSP 固件和设备校准分区。\
> 9. 收货后如果无法安装 APK，或麦克风/扬声器异常，是否支持退换？

不要让销售承诺“支持 HA”、“支持端侧 AI”或“支持 AEC”，普通销售无法可靠验证这些技术能力。只让他们回答开机、联网、ADB、APK、root 实测、拆修、固件回刷和退换。

### 9.3 ADB、Android root 与 Rockchip 刷机权限

三者不是一回事：

| 能力 | 准确含义 | 验证方式 |
|---|---|---|
| 普通 ADB | 电脑能连接 Android 的 `adbd`，普通 `adb shell` 可能只是 `shell` 用户 | `adb connect` 成功，`adb shell id` 常见为非 0 UID |
| Android root | 当前 Android 系统可以通过 `su` 获得 `uid=0` | `adb shell su -c id` 输出 `uid=0(root)` |
| root ADB | `adbd` 本身以 root 运行，开发最方便 | `adb shell id` 直接输出 `uid=0(root)` |
| Loader/Maskrom | 拆机通过 Rockchip 恢复通道读写 eMMC | Rockchip 工具识别设备并能读取设备/分区信息 |

Android root 只针对当前 Android 系统，不会自动解锁 DSP，也不会在刷成新 Linux 后继承。可以要求销售提前 root，但必须保留原厂 Android 音频底层，不接受来历不明的远控程序、第三方账号或仅靠 KingRoot/Magisk 图标作为证据。到货后要重置 root 管理配置，开发完成后关闭公网/跨 VLAN 的 5555 访问。

### 9.4 有 ADB 时请销售回传的结果

```bash
adb connect 音箱IP:5555
adb shell getprop ro.build.version.release
adb shell getprop ro.build.version.sdk
adb shell getprop ro.build.display.id
adb shell getprop ro.product.cpu.abi
adb shell getprop ro.product.model
adb shell id
adb shell su -c id
adb shell getenforce
adb shell df
adb shell pm list packages
```

合格结果是：`adb connect` 成功，Android 预期为 5.1/API 22，能列出包和剩余存储；具备 root 的 R1 真机还必须至少在 `su -c id` 输出 `uid=0(root)`。销售不会操作时，只购买一台支持无理由退货的 R1 真机，不直接下单 3～8 台。

### 9.5 采购分级和到货准入

| 级别 | 条件 | 决策 |
|---|---|---|
| A | ADB 可连、APK 可安装、音频/按键正常、可退换 | 优先购买首台 R1 真机 |
| B | ADB 可连，普通 `adb install` 不确定，但可退换 | 仅购买一台 R1 真机，在退货期内完成开发验证 |
| C | 无 ADB，销售可拆机备份、开启 ADB 并提供回刷 | 免拆货源不可得时的备选，仍只买一台 |
| D | 无 ADB、无可验证拆机方案、不支持退换 | 不购买 |

到货后先在退货期内完成：调试APK安装、录音/播放数据链、输入源、原厂AEC配置与参考/处理数据、KWS运行事件和24小时稳定性。只有首台R1真机通过现行功能、数据、链路、安全、恢复与稳定性门槛，才考虑同来源其他设备；不要求用户执行第14章真人声学验收。

## 10. 免拆实机教程

> 本节命令默认只读。把 `192.0.2.10` 替换为 R1 实际 IP。不要在完成备份前执行 `pm hide`、卸载、清缓存或刷机命令。

### 10.1 前期准备

- 一台与 R1 同网段的 Windows/macOS/Linux 电脑。
- Android Platform Tools，并确认 `adb version` 正常。
- 路由器后台能看到 R1 IP/MAC。
- 至少 10 GB 的本地备份空间。
- 记录 R1 当前配网、蓝牙、DLNA、按键、灯效和播放状态。

### 10.2 检查端口和 ADB

```bash
nmap -sT -p 5555,6053,8000-9000 192.0.2.10
adb connect 192.0.2.10:5555
adb devices -l
```

若 5555 关闭，不要反复尝试来历不明的一键工具，转到 10.6 的决策分支。

### 10.3 收集系统信息

```bash
adb shell getprop ro.build.version.release
adb shell getprop ro.build.version.sdk
adb shell getprop ro.build.display.id
adb shell getprop ro.product.cpu.abi
adb shell getprop ro.product.model
adb shell getenforce
adb shell id
adb shell cat /proc/cpuinfo
adb shell cat /proc/meminfo
adb shell df -h
adb shell pm list packages -f
adb shell ps
```

保存输出。若设备的精简 shell 不支持 `df -h` 或 `ps` 参数，改用不带参数版本。

### 10.4 收集音频和输入设备信息

```bash
adb shell cat /proc/asound/cards
adb shell cat /proc/asound/pcm
adb shell dumpsys audio
adb shell dumpsys media.audio_flinger
adb shell getevent -lp
```

部分命令可能因权限失败；失败本身也是结论，记录错误即可，不要立即 root。

### 10.5 备份原 APK

先找路径：

```bash
adb shell pm path com.phicomm.speaker.device
adb shell pm path com.phicomm.speaker.player
adb shell pm path com.phicomm.speaker.launcher
```

然后对返回的每个绝对路径执行：

```bash
adb pull /system/app/实际文件.apk ./r1-backup/
```

同时保存 `getprop`、包列表和校验值。普通 ADB 无法读取分区镜像时，不把 APK 备份误称为“完整固件备份”。

### 10.6 免拆决策

| 结果 | 下一步 |
|---|---|
| ADB 可连、`adb install` 可用 | 安装独立真机调试 APK，不碰原厂包 |
| ADB 可连、普通安装失败 | 记录完整错误，检查精简包管理器/签名/API 兼容 |
| 5555 关闭、已有可信免拆 OTA 路线 | 核对当前版本、目标版本、校验值和回退包后再做 |
| 无 ADB、无可信免拆升级 | 停止软件操作并记录免拆路线阻塞；当前首台不进入拆机兜底 |

### 10.7 安装真机调试 APK

```bash
adb install -r r1-audio-probe.apk
adb shell am start -n 你的包名/.MainActivity
adb logcat
```

真机调试 APK 的音频首阶段只做三件事：列出音频能力、录制测试 WAV、播放测试音。TCP/Assist 连接属于阶段 2，不把尚未实现的连接能力计入音频 APK 验收。若当前第三方音频服务运行时阻塞独立 APK 录放音，应在确认目标固件、APK 备份和恢复命令后临时逐个停止、逐项验证；不要在真机开发阶段 `disable`、卸载或覆盖当前更新包或系统基础 APK。

## 11. 拆机兜底说明

> 2026-09-11现行决定：本章保留为通用历史参考，不适用于当前`r1-sample01`开发与验收。
> 当前首台不打开机壳、不短接、不焊接，也不把物理Maskrom作为前提；免拆路线出现硬件
> 限制时记录可复现证据并交由用户决定，不自动转入本章步骤。

### 11.1 为什么拆机

拆机不是为了换扬声器，也不是默认开发步骤。唯一目的应是：原固件没有可用 ADB/安装通道时，通过主板预留 USB 触点进入 Rockchip 刷机/恢复通道，备份分区并刷入可调试固件。

### 11.2 委托第三方的工作单

要求操作人员：

1. 拆机前拍摄外观和功能视频。
2. 小心拆除底部和主板，保护灯效/触摸排线。
3. 根据已验证资料连接预留 USB 数据触点，不凭猜测接电源线。
4. 先读取设备信息和可读分区，保存原始镜像及 SHA-256。
5. 刷入明确适配 R1/RK3229、可回退、启用 ADB 的固件。
6. 不随意替换音频 DSP 固件、Audio HAL 和校准数据。
7. 完成开机、Wi-Fi、ADB、麦克风、扬声器、按键和灯效测试。
8. 交付接线图、工具版本、镜像、校验值和回刷步骤。

验收底线是“可进入 ADB 且原音频通路仍工作”，不是仅仅看到设备能开机。

### 11.3 完整备份与 Linux 实验红线

拆机后先使用与实机匹配的 Rockchip 工具保存整个 eMMC 或全部可读分区，至少单独保留 loader/uboot、trust、boot、recovery、system、misc、分区表、内核命令行和 DTB，并为每份输出记录 SHA-256。同时导出 `/system/lib/hw`、音频配置、内核模块、DSP/Wi-Fi 固件和校准数据。备份完成不等于可恢复；必须至少验证分区表、镜像尺寸、校验值和实际回刷启动流程。

任何 Linux 实验都不允许直接覆盖唯一的 eMMC 原系统。优先使用 RAM/临时介质启动，复用原厂内核、DTB 或经提取验证的板级参数。只有同时通过稳定启动、Wi-Fi、麦克风真实通道、扬声器/功放、全双工和 AEC 测试，且声学表现不弱于原系统时，才能把 Linux 升格为候选生产系统。

## 12. 智能中枢语音卫星扩展能力

下表保留扩展规划；其中单台配置管理、静音/监听指示、签名更新与回滚已由 2026-09-07 决策纳入当前单台功能，以第 5、13 章及功能盘点为准。免打扰是当前明确需求，不等同于表中的自动耳语识别。其余后续能力不混入生产首版硬性验收。优先级中 P1 表示适合第二阶段，P2 表示需单独设计，实验性表示受 R1 硬件或声学环境强约束。

| 类别 | 扩展能力 | 用户价值 | 主要依赖 | 优先级 |
|---|---|---|---|---|
| 交互 | 多唤醒词/家庭成员专属唤醒词 | 不同人可触发不同助手或场景 | 端侧多 KWS 模型和远程配置 | P1 |
| 交互 | 说话人识别与个性化 | 按成员提供日程、音乐和权限 | 声纹模型、家庭成员注册、隐私同意 | P2 |
| 交互 | 耳语/夜间模式 | 自动识别低声说话并降低回复音量 | 音量估计、时段/区域策略 | P1 |
| 交互 | 断网基础指令 | HA 或网络中断时仍可停止、静音、调音量 | APK 本地固定意图 | P1 |
| 空间智能 | 同区域多卫星仲裁 | 多台听见同一指令时只执行一次 | 检测时间、RMS、优先级交换 | P1 |
| 空间智能 | 声源方向/距离估计 | 优先由靠近用户的设备响应 | 可访问的多麦通道、阵列算法 | 实验性 |
| 空间智能 | 跨房间会话接力 | 从主卧走到书房后继续同一会话 | 会话所有权转移、占用/人体信号 | P2 |
| 主动智能 | 基于人员/区域的提醒 | 只在目标人所在房间播报 | 人体存在、BLE/手机定位、HA 自动化 | P1 |
| 主动智能 | 异常状态主动追问 | 如门长时间未关时询问是否处理 | HA 事件、`START_CONVERSATION`、确认流程 | P1 |
| 知识/AI | 家庭文档 RAG | 查询说明书、家庭规则、维修记录 | 文档索引、权限和来源引用 | P1 |
| 知识/AI | 网址、本地文件和资源库调用 | 搜索新闻、菜谱、自建音频和其他内容 | HA 工具层、白名单、超时和来源说明 | P1 |
| 知识/AI | 本地/云端模型智能路由 | 简单指令快速本地执行，复杂问题选更强模型 | 路由策略、健康检查、费用/隐私上限 | P1 |
| 知识/AI | 短期/长期记忆 | 记住偏好、未完成事项和对话背景 | 可审查存储、过期和删除机制 | P2 |
| 通信 | 房间对讲/家庭留言 | 从一个 R1 呼叫、对讲或给其他房间留言 | 音频会话、目标路由、录音同意 | P2 |
| 通信 | 全屋广播/紧急呼叫 | 家庭通知和紧急情况快速触达 | 高优先级播放、回执、权限 | P1 |
| 媒体 | Music Assistant/自建曲库 | 中文语音搜歌、续播、播放队列 | 媒体元数据和 HA `media_player` | P1 |
| 媒体 | 播客、有声书和进度记忆 | 跨设备续听 | 内容源、用户进度库 | P1 |
| 媒体 | Snapcast 严格多房间同步 | 多台音箱减少回声和拍频 | API 22 Snapclient、时钟校准 | P2 |
| 媒体 | 两台 R1 左/右声道 | 形成真正的立体声声像 | 声道拆分、固定角色、DSP 延迟校准 | 实验性 |
| 环境 | 噪声等级/异常声音检测 | 自动调节唤醒阈值，识别烟雾器/玻璃碎裂等 | 本地特征提取、隐私策略 | P2 |
| 运维 | HA 远程配置和批量管理 | 统一修改唤醒词、音量、日志和 Area | 配置实体、设备分组、审计 | P1 |
| 运维 | APK/模型 OTA、回滚和健康监测 | 多台终端可安全更新和自恢复 | 签名、A/B 或备份位、指标上报 | P1 |
| 隐私 | 物理静音、监听指示和音频留存策略 | 用户能明确知道何时在听、何时上传 | 按键/灯效、本地默认、诊断录音明示开关 | P1 |
| 安全 | 高风险操作二次确认 | 开门、解锁、付费或删除前再确认 | 用户/声纹、HA 权限、超时 | P1 |

## 13. 分阶段开发路线

### 2026-09-07 当前执行顺序：先补齐单台功能，再统一验证

全链路优化已纳入[O1～O8开发任务](docs/2026-09-07-r1-chain-optimization.md)，尚未因文档整合实现或验收。O1随功能加入分段诊断；O2为统一播放基础，O3流式结合O5会话/O6兼容适配推进；O4随双入口与闹钟同步实现；O8随流式机器测量评估，O7以固定输入、协议事件和状态回读闭环。上述依赖不替代硬件与配网/蓝牙实机功能检查，无设备可继续独立开发。

用户决定暂停意图与语音链路调试、断网/IP 变化恢复检查及 72 小时稳定性验收。当前优先补齐首台 R1 的功能实现：播放中打断、流式回答、主动播报与计时器、本地闹钟、免打扰、媒体播放、手机蓝牙音箱、安卓/iPhone 自助配网、按键/物理静音/灯效、设备名称/区域与系统管理、签名更新与回滚。必要配置与状态统一支持语音/HA 双入口，新增需求不代表能力已交付。

先验证硬件入口及配网/蓝牙可行性，再补齐统一播放、打断、流式回答、主动播报和媒体；接入本地闹钟/计时器、免打扰与双入口管理，再补齐硬件、手机配网、蓝牙音箱及系统更新恢复。HA 内容平台与视频电视播放接入后续在 HA 实施，不阻塞 R1 基础功能。

单台功能实现齐备后，针对最终候选版本集中进行功能/数据/链路自动回归、断网/IP变化恢复检查，最后进行72小时稳定性验收；发现问题则修复并回归受影响项。开发期间继续与风险相称的构建、单元测试和最小实机功能检查，不将代码完成或纯模拟通过当成设备交付。

“好”、“是→4”、提问理解偏差和历史偶发误触发保留为历史诊断，真人复测按2026-09-12决定统一跳过。第14章真人/主观指标数值不改写但标记未验收，不再阻塞当前首台；功能、数据、链路、安全、恢复和稳定性门槛继续执行。当前不扩大到多台部署或严格同步音乐/左右声道实验，不重启KWS训练、选型及专项声学测试。

**2026-09-08 优先级更新：** 在上述首台范围内，原厂四麦和音频处理链攻关立即前置。先完成完整 eMMC 备份/回刷门槛，再以 Root、特权代理和定制系统环境打通原厂 MicArray、DOA、AEC及DSP处理，随后继续其余单台功能。该调整不恢复 KWS训练或多台部署，也不把接口存在、Root成功或合成测试写成声学验收通过。

**2026-09-11授权更新：** 当前首台始终免拆。R0继续独立保持`pending`；后续可在较低权限路线有不足证据后，按精确目标分区双读、候选固定、单次写入及复位前读回门禁依次使用必要的boot、system或recovery修改。电气硬断麦和全部引导组件同时损坏后的恢复不再阻塞其余功能验收；Loader、分区表、首4 MiB、擦除、格式化与整盘覆盖仍须另行授权。

### 阶段 0：实机盘点（0.5～1 天）

- 完成第9～10章的采购准入和免拆检查；第11章不适用于当前首台。
- 输出固件/API/ADB/root/Loader/音频能力表。
- 门槛：当前首台固定为免拆路线；确认ADB、免拆升级/Loader入口及与目标操作相称的备份和哈希门禁，R0未完成项继续标记`pending`。

### 阶段 1：音频真机开发与验证（2～5 天）

- 先由独立 APK 录制 16 kHz PCM、播放 WAV/TTS，测试三种 AudioSource 以及系统 AEC/NS/AGC，并逐个隔离当前第三方音频包。
- 已完成标准 AudioRecord 与系统 `Uni4micHalJNI/libuni4michal` 私有四麦链路对照准入；普通 APK 直连因 SELinux 权限被拒绝。2026-09-08 起私有四麦链转由完整回刷保护下的 Root、专用守护进程和受限 SELinux 域继续攻关，标准 AudioRecord 保留为对照，不再自动成为生产默认。
- 历史音频声学子门槛为1米固定短语、10dB和用户可闻确认；自2026-09-12起不再要求用户复测或继承该主观结论。当前强制项为原厂输入/输出数据非空、格式正确、无持续丢帧/削波及播放状态可回读。
- 音频子门槛已经通过；KWS 于 2026-09-04 从零重启，旧模型和证据不再作为新选型输入。
- 按第 5.5 节依次完成四条免费离线路线的许可审计、主机可行性、API 22/armeabi-v7a 准入、统一盲测和 R1 真机测试。
- Alexa 真人验证和冻结后盲测必须分会话采集；当前单人结果允许用户定型，但跨说话人泛化不得标记为通过。
- 分别记录 CPU、PSS、实时率、温度、误/漏唤醒，再对入选模型连续运行 24 小时。
- 门槛：没有持续丢帧、内存泄漏、热降频或后台被永久杀死，并选定一款进入声学验收的 KWS 配置。

**2026-09-05 用户决策更新：** 直接采用现有 Alexa microWakeWord v2，停止 KWS 训练、选型及追加声学测试，允许推进阶段 2 开发。既有 1 米 4 次说词/4 次事件仅为功能试测；未完成的正式声学、误唤醒和稳定性门槛仍标为未验证，不改写第 14 章指标或宣称阶段 1 总验收通过。旧训练产物已清理，保留选定权重和运行依赖。

### 阶段 2：Assist WebSocket 闭环（3～7 天）

- 唤醒后将预缓冲和 PCM 上传 HA，收到中文 STT/Intent/TTS。
- 实现停止、超时、断线重连和 `conversation_id`。
- 门槛：安静环境 30 次命令成功率 ≥95%，P95 回复起始延迟达到可接受基线。

2026-09-05 版本 39 开发偏差：用户停顿后命令被 HA VAD 提前截断，且 Whisper 将 Alexa 转写成中文混合文字。上传预缓冲由2秒缩为200ms，STT 禁用 HA VAD，由客户端保证至少8秒输入窗口，安静1.5秒后结束、最长20秒。此能量启发式及短缓冲待实机反馈；增加的响应等待计入延迟指标，不降低原验收门槛。见 `docs/2026-09-05-assist-command-window.md`。

2026-09-05 版本40交互更新（取代上一条版本39端点策略）：按用户要求，Alexa后本地随机回应，关闭录音避免提示语自采集；播放结束后新建录音并等待开口6秒，WebRTC VAD确认人声才启动HA。无命令静默退出，有命令按1.2秒尾部静音结束、最长20秒，不再固定等满8秒。日常/偶尔回应采用用户选定6+4句及约85%/15%长期占比。当前不支持提示语中插话、Alexa与命令连说；完整实机交互及正式阶段门槛仍未验证。见 `docs/2026-09-05-assist-acknowledgements.md`。

2026-09-05 版本42输入判定：不再只用纯标点规则；根据官方Whisper/HA资料实现分层无有效输入策略，区分无输入、可疑识别与真正意图失败。保护单字数字和HA明确追问中的回答，只在弱VAD证据与本次提示语/高度重复文字同时满足时过滤疑似幻觉。HA未透传ASR置信度，不伪造概率或宣称覆盖全部噪声误识别。规则、阈值、依据和例外见 `docs/2026-09-05-assist-input-policy.md`；实机声学效果仍待使用验证。

### 阶段 3：原生卫星协议（2～4 周）

2026-09-06 版本47交互更新：等待开口/结束停顿/单条命令默认分别20/1.8/30秒，均通过HA配置并由R1执行；支持音量和0.5～1.5倍语速（默认0.85），本地提示及专用HA TTS统一变速。新增已选定的进入/结束语音提示；保留首次无输入静默退出。原生输入与播放上限同步扩展，隐藏原HA无效VAD选项及第二助手/唤醒槽位，主Alexa配置同步恢复。119项Android测试、32项HA测试、真实配置/音量/TTS及重启检查通过；真人可闻性及72小时验收仍待确认。详见 `docs/2026-09-06-r1-interaction-settings.md`。

2026-09-06 版本46更新：普通shell的用户级 `hide/unhide` 在本机可用，原两包APK/数据保留、启用状态不变；两包及子进程退出。隐藏、解除隐藏恢复及连续三次R1重启通过，每次无需人工启动即可恢复HA连接和连续真实音频帧（约31～41秒）。卫星API22开机检查已识别隐藏状态，部署工具保存隐藏基线并在解除隐藏前等待自身音频释放。该结果替代下文版本44/45的持久隔离阻塞结论，不代表真人完整问答、网络恢复或72小时总验收通过；详见 `docs/2026-09-06-r1-nonroot-isolation.md`。

2026-09-06 版本45更新：优先争取原音频两包进程完全退出，无法实现再验证音频独占；允许评估root可行性，但保留设备确认与备份/回刷方案前提。新增API22开机隔离检查及持久阻断原因，临时force-stop明确不等于生产隔离；部署工具以实际包状态写入及回读验证管理权限。后续经用户明确豁免本次恢复前提，标准ADB root实际尝试一次，虽然提示重启为root，实际UID仍为2000；普通重启后恢复调试属性，未取得root。永久停用、音频独占后备方案及正式重启/72小时门槛仍未通过。详见 `docs/2026-09-06-r1-isolation-guard.md`。

2026-09-06 原生运行集成：版本44已连接前台服务、私有身份/Noise密钥、6053监听、mDNS、真实AudioRecord/AudioTrack、开机入口及HA显式配对，独立原生中文助手已创建，原默认助手保留。用户确认旧WebSocket链路可用，不重复该链路测试。113项Android测试、24项HA测试及合成协议检查通过；临时隔离下真实录放资源检查通过，正式真人问答、量化指标与72小时验收未通过或未执行。

**实际部署偏差：** 固件3448普通shell不能修改原音频包启用状态，`disable-user`切换失败；临时停止两包后重启，原启动流程仍会恢复它们。原包运行场景出现真实AudioRecord停止Binder阻塞，不能用进程自启或短暂listening代替音频恢复。新代码把阻塞停止操作移出协议线程，清理超时后恢复为持久audio_blocked管理模式，等待管理员排除音频占用后重试。卫星专用部署门槛仍未通过；当时决定不因该发现扩大为root、系统APK替换或分区修改，该限制已由2026-09-08原厂音频优先决策取代，本段仅保留历史证据。详见 `docs/2026-09-06-native-satellite-runtime.md` 与对应证据目录。

2026-09-06 端点/续听开发：独立STT模式通过 `requires_external_vad=false` 接管输入结束，增加EOF、格式、容量与时间保护；会话适配继续委托现有家庭助手路由。R1正常回复后自动续听，6秒无输入返回唤醒监听、保留120秒短期上下文（从最近请求计时，与现有Hub一致）。19组HA与110项Android测试及R1合成两轮互通通过，未切换现有助手或验收真实语音。记录见 `docs/2026-09-06-native-endpoint-dialogue.md`。

2026-09-06 开发记录：原生音频协调与API22播放适配代码已实现，主机/R1合成双向互通通过，尚未启动真实音频或配对。使用160KB上传队列、32KiB下行缓冲和独立播放排空判定。HA默认0.7秒VAD端点可能先于端侧1.2秒结束；正式接入前须在独立路径解决，不改变现有助手、不降低交互指标。证据与未验收边界见 `docs/2026-09-06-native-audio-runtime.md`。

2026-09-05 输入边界补充：采用可选 `r1_input_guard` STT包装实体，在成功识别结果返回管线之前过滤空白/标点和非语音标记，由HA的 `stt-no-text-recognized` 路径终止意图和TTS。不修改Core，不在STT接口假设存在追问/VAD上下文。同版本容器的规则、原版管线调用边界与实体生命周期检查通过；生产尚需选择该实体，R1全链路未验收。详见 `docs/2026-09-05-ha-input-guard.md`。

2026-09-05 开发进展：Noise/基础控制通道之后新增原生命令PCM上传与会话状态组件，JVM及R1完成官方客户端合成音频互通；87项主机单元测试通过。此为阶段3技术预研证据，不代表阶段2/3门槛通过。真实收音、TTS、实体配对与常驻仍未实现；原生管线须在HA意图执行前落实无有效输入拦截，不能依赖收到STT事件后取消来阻止意图执行。固定协议与客户端订阅位差异、证据和边界见 `docs/2026-09-05-native-voice-session.md`。

- 实现 ESPHome API 最小服务端、mDNS 和 Noise。
- 注册 `assist_satellite`、`media_player` 及配置实体。
- 实现 announce、start conversation、计时器、增量 intent/流式 TTS 和播放完成回报。
- 门槛：HA 重启、APK 重启、IP 变化后自动恢复，实体 ID 稳定。

### 阶段 4：硬性交互能力与声学调优（2～4 周）

2026-09-12 当前首台及后续的非专业真人定位、声学和主观体验验收按用户决定跳过；生产链
仍须使用原厂音频处理结果，并通过可自动复核的功能、数据、链路、安全、恢复和稳定性检查。
第14章相关历史指标保留但标记未验收，不以跳过冒充通过。

- 在阶段 1 原厂音频路线成果上集成特权代理与普通卫星 APK，完成四麦、DOA、播放参考、AEC和音频所有权的生产生命周期。
- 固定由用户根据统一报告选定且通过 R1 实测的 Alexa KWS 模型，完成远程配置和签名更新。
- 实现回复后续听、多轮会话、长答案流式播放、主动播放、TTS 插话和音乐中唤醒。
- 调优 VAD、AEC、NS、AGC、停止词和中文端点。
- 门槛：现行功能、数据、链路、安全、恢复和稳定性指标通过；第14章依赖真人/主观条件的效果指标保留为用户跳过/未验收。原厂链功能或数据不通时继续穷尽允许的高权限路线，不以跳过掩盖链路失败。

### 阶段 5：3～8 台和区域路由（1～2 周）

- 完成命名、Area、主监听设备、通知组和媒体组。
- 压测 8 路音频和并发会话。
- 门槛：不串流、不重复执行，断线不影响其他卫星。

### 阶段 6：同步音乐实验（另立项目）

- 先验证 Snapclient 在 API 22 和 R1 音频输出上稳定运行。
- 再测组播同步，最后才做左右声道拆分。
- 不阻塞语音卫星首版交付。

## 14. 测试与验收

### 14.1 功能测试

- 英文/中文唤醒词与全中文 STT、意图、对话和 TTS，不得因英文唤醒词把管线切成英文。
- 单轮、5 轮上下文连续对话、回复后 10 秒免唤醒续听和窗口超时。
- 不少于 60 秒的长回复边生成边播放，工具调用造成短暂文本间隔时不得卡死。
- 本地/云端对话路由、本地资源文件和允许网址的调用，失败时向用户说明而不静默。
- 按键停止、语音停止、新唤醒打断、TTS 中直接插话和中文句子端点。
- 音乐中普通聊天不打断，唤醒词/停止词能打断，命令后正确恢复原播放状态。
- 定向通知、全屋通知、主动发起对话。
- 当前区域控制、明确指定其他区域。
- R1/HA/路由器分别重启后的恢复。

### 14.1.1 新增功能与双入口管理验收

2026-09-07 优化验收补充：按[O1～O8 场景](docs/2026-09-07-r1-chain-optimization.md)检查取消与迟到事件隔离、媒体恢复、至少 60 秒真流式、工具确认后反馈、同值旧缓存/并发/离线设置、会话独立过期、HA 兼容加载卸载及转换/缓冲压力。主机/模拟与真实后端/实机分别记录；交接须关联完整提交、APK SHA-256、HA Core 和相关自定义组件/后端版本。分段诊断默认不保存正文、录音或凭据，容量与开销有界；使用各端单调时间，跨端未经校准不相减。未来完整验收排期不因登记场景提前。

以下为后续实现的新增验收场景，不表示本次文档更新已运行检查或改变历史结果。

- 语音修改后 HA 更新、HA 修改后语音查询一致；名称/Area 修改保持设备身份，重启持久化，并发修改后收敛，非法值拒绝且不覆盖旧值。
- 失败、离线、待同步、过期或不可读状态如实反馈，只读不可写，物理静音不被绕过，凭据不被朗读或展示。
- 语音与 HA 创建的全部闹钟在 HA 可见；修改、取消、停止、稍后提醒和启停双向同步，已同步闹钟离线执行，重启/校时/时区及时间无效处理正确，重连不重复响铃。
- 免打扰手动/定时、跨午夜、主动语音和闹钟例外、本地执行及过期通知处理正确。
- 安卓/iPhone 首次配网、换网、错误密码、退出及失联恢复；蓝牙配对、断开、播放及各音频来源抢占/恢复；按键、静音与状态灯须实机验证。
- 服务与整机重启分别记录，签名/哈希错误拒绝及更新失败恢复通过；现有人工部署工具不替代自动更新验收。
- HA 内容接入后验证视频精确名称、多候选、随机/评分筛选、目标电视歧义、无结果及播放失败反馈。

### 14.2 声学测试

- 1 m、3 m、5 m；正面、侧面、背面。
- 安静、电视、音乐、厨房、人声背景。
- TTS 和音乐分别在 30%、60%、90% 音量下唤醒。
- 记录原始 WAV、处理后 WAV、RMS、削波率和识别结果。

### 14.3 性能指标

2026-09-07 已知差距：当前源码默认尾部静音为 1.8 秒，与下表正常句末 0.5～1.2 秒及 STT 完成 P95 ≤1.5 秒目标存在差距；设备保存值须实际核验。本次不改设置、不改指标、不宣称达标，列入 O7 最终候选回归与修复闭环。O1 分段测量区分首段/全文、首块写入/实际发声和流结束/硬件排空，不能将可重叠的阶段耗时简单相加冒充端到端耗时。

| 指标 | 生产验收目标 |
|---|---:|
| 安静环境命令成功率 | ≥95% |
| 正常家庭背景命令成功率 | ≥90% |
| 正常环境唤醒成功率 | ≥95% |
| 误唤醒 | ≤1 次/8 小时/设备 |
| 音乐 60% 音量唤醒成功率 | ≥90% |
| 音乐 90% 音量唤醒成功率 | ≥85% |
| 本地 KWS 检测延迟 P95 | 唤醒词结束后 ≤500 ms |
| APK 常驻 PSS | 目标 ≤120 MB，且系统不发生持续内存回收/重启 |
| KWS 实时能力 | 实时率 ≤0.5，24 小时无热降频 |
| 中文端点延迟 | 正常句末 0.5～1.2 秒，过早截断率 ≤2% |
| 语音结束到 STT 完成 P95 | ≤1.5 秒，按实际硬件复核 |
| 家居指令语音结束到首个 TTS 音频 P95 | ≤2.5 秒 |
| AI 对话语音结束到首个 TTS 音频 P95 | 本地/云端正常时 ≤5 秒 |
| 长回复流式播放 | 首句可播即开始，60 秒回复无丢句、重播、卡死 |
| 停止指令/按键到 TTS 静音 | ≤300 ms |
| TTS 自由插话到旧音频静音 P95 | ≤500 ms |
| 5 轮连续对话成功率 | ≥95%，上下文不丢失 |
| 主动播放 | 30 次通知/对话全部完整播完并正确回报 |
| 断网恢复 | 网络恢复后 30 秒内 |
| 72 小时生产稳定性 | 无崩溃、无持续内存增长、无音频设备占用死锁 |

播放中唤醒、TTS 插话、流式回复或智能端点任一项未达标，即为生产验收失败。安静环境的高成功率不能抵消播放场景不达标。

### 14.4 多设备测试

- 1、3、8 台逐级压测。
- 每台设备 ID 和实体 ID 唯一且重启不变。
- 当前房间默认回复，不自动全屋回答。
- 同一区域只启用主监听设备时无重复执行。
- 多设备同时发出不同命令时音频和会话不串线。
- 空闲时确认 8 台都不上传环境 PCM；8 路并发命令时记录 HA/PVE CPU、内存、网络和事件循环延迟。

## 15. 风险、回退与最终建议

| 风险 | 影响 | 应对 |
|---|---|---|
| 原固件无 ADB | 无法安装 APK | 使用可信免拆升级；当前首台不拆机，入口耗尽后记录阻塞 |
| 把 ADB、Android root 和 Maskrom 混为一谈 | 采购验收通过但仍无法开发/恢复 | 分别执行 `adb connect`、`id`/`su -c id` 和 Rockchip 识别/读取测试 |
| 原语音服务独占麦克风 | 真机调试 APK 无法录音 | 先备份，再单独停止冲突服务；必要时系统权限/root |
| 私有 DSP/AEC 普通 APK 不可复用 | 播放中唤醒和 TTS 插话失败 | 按免拆分级授权依次验证Root代理、受限SELinux域、boot/system/recovery和最小原厂服务桥；全部形成可复现失败证据后才提交用户决定是否采用自研处理降级 |
| 端侧 KWS 超出 RK3229 资源 | 发热、卡顿、被杀或漏唤醒 | 对四条免费离线路线统一测量；优先选择满足实时率和内存约束的模型 |
| KWS 框架与模型许可混淆 | 公开/商业分发存在合规风险 | 保留 Apache-2.0 声明，并对每个模型权重、词表和训练数据单独核对授权 |
| ESPHome API 版本变化 | APK 失联 | 固定兼容版本、协议测试、跟踪 api.proto |
| 唤醒模型效果差 | 误唤醒/漏唤醒 | 英文短语优先、换短语/模型、真实房间调参 |
| 中文 STT 或端点不稳定 | 识别错误或语句被截断 | 显式使用 `zh`、中文优化 STT，本地 VAD 与 HA VAD 双重判定 |
| 对话/TTS 后端不支持真流式 | 长答案需要长时间等待 | 验证 `intent-progress` 和流式音频；换兼容后端，非流式仅作诊断回退 |
| HA VM 推理争用 | 延迟、自动化卡顿 | 增加 PVE 资源或独立推理 VM |
| 多设备重复听到命令 | 重复执行 | 每区域一台主监听；后续再做仲裁 |
| 普通组播不同步 | 回声和拍频 | 明确区分组控与同步；需要时上 Snapcast |
| 刷机破坏音频配置 | 音质/功能丢失 | 每个目标分区先双读备份并固定哈希，最小化修改，写后读回；不把未验证恢复能力写成可恢复 |
| 通用 RK3229 Linux 只能启动基础系统 | 四麦、DSP、AEC、功放或 Wi-Fi 不可用 | 只作 RAM/临时介质实验；所有音频门槛通过前不覆盖 Android |

最终建议：继续只使用首台`r1-sample01`开发并始终免拆。R0完整eMMC、独立入口和受控回刷继续独立保持`pending`；按[免拆分级授权](docs/2026-09-11-r1-no-disassembly-authorization.md)和[原厂音频优先路线](docs/2026-09-08-r1-factory-audio-root-plan.md)，在目标分区安全门禁下依次穷尽Root代理、SELinux、boot/system/recovery及原厂服务桥方案，实际复用四麦、AEC和DSP处理。复杂或需要已授权分区刷写不构成停止理由；所有免拆原厂路线均失败时，先提交证据并由用户决定是否接受标准AudioRecord加自研处理。原厂音频链及全部硬性交互能力通过最终语音、播放和72小时验收后，再批量采购和部署。

## 16. 资料来源

### Home Assistant 与协议

- [Home Assistant：Assist satellite entity](https://developers.home-assistant.io/docs/core/entity/assist-satellite/)
- [Home Assistant：Assist pipelines](https://developers.home-assistant.io/docs/voice/pipelines/)
- [Home Assistant：Wyoming Protocol](https://www.home-assistant.io/integrations/wyoming/)
- [ESPHome：Voice Assistant](https://esphome.io/components/voice_assistant/)
- [ESPHome：Micro Wake Word 端侧唤醒](https://esphome.io/components/micro_wake_word/)
- [ESPHome：Native API](https://esphome.io/components/api/)
- [ESPHome api.proto](https://github.com/esphome/esphome/blob/dev/esphome/components/api/api.proto)
- [Home Assistant Core：ESPHome Assist Satellite 实现](https://github.com/home-assistant/core/blob/dev/homeassistant/components/esphome/assist_satellite.py)
- [OHF Voice：Linux Voice Assistant](https://github.com/OHF-Voice/linux-voice-assistant)
- [已归档的 Wyoming Satellite](https://github.com/rhasspy/wyoming-satellite)

### 本地语音与唤醒

- [Home Assistant：本地语音助手配置](https://www.home-assistant.io/voice_control/voice_remote_local_assistant/)
- [Home Assistant Whisper App 文档](https://github.com/home-assistant/addons/blob/master/whisper/DOCS.md)
- [Home Assistant Piper App 配置与中文音色](https://github.com/home-assistant/addons/blob/master/piper/config.yaml)
- [Home Assistant openWakeWord App 文档](https://github.com/home-assistant/addons/blob/master/openwakeword/DOCS.md)
- [openWakeWord 上游项目与语言说明](https://github.com/dscripka/openWakeWord)
- [sherpa-onnx 开放词表 Keyword Spotting](https://k2-fsa.github.io/sherpa/onnx/kws/)
- [sherpa-onnx：Android 编译与 ABI 支持](https://k2-fsa.github.io/sherpa/onnx/android/build-sherpa-onnx.html)
- [sherpa-onnx：KWS Android APK](https://k2-fsa.github.io/sherpa/onnx/kws/apk.html)
- [sherpa-onnx：KWS 预训练模型总表](https://k2-fsa.github.io/sherpa/onnx/kws/pretrained_models/index.html)
- [sherpa-onnx：KWS 模型发布页](https://github.com/k2-fsa/sherpa-onnx/releases/tag/kws-models)
- [sherpa-onnx：项目与 Apache-2.0 代码许可](https://github.com/k2-fsa/sherpa-onnx)
- [sherpa-onnx KWS 模型商业使用与再分发许可澄清记录](https://github.com/k2-fsa/sherpa-onnx/issues/3760)

### Android 音频

- [Android：MediaRecorder.AudioSource](https://developer.android.com/reference/android/media/MediaRecorder.AudioSource)
- [Android：AudioEffect](https://developer.android.com/reference/android/media/audiofx/AudioEffect)
- [Android：Audio Focus](https://developer.android.com/media/optimize/audio-focus)

### R1 与多房间音频

- [斐讯 R1 公开拆解与硬件信息](https://news.mydrivers.com/1/574/574576_1.htm)
- [斐讯 R1 公开产品参数](https://detail.zol.com.cn/1210/1209731/param.shtml)
- [R1 Helper：斐讯 R1 第三方语音 APK 实践](https://github.com/sagan/r1-helper)
- [VRISE/feixun-r1：Android 5.1、ADB 与原包注入实践](https://github.com/VRISE/feixun-r1)
- [pexcn/phicomm-r1-ota：R1 OTA 和 ADB 实践](https://github.com/pexcn/phicomm-r1-ota)
- [RK322x 通用 Linux 4.4 内核与电视盒子 DTB 实践](https://github.com/hexdump0815/linux-rockchip-rk322x-kernel)
- [Snapcast：同步多房间音频](https://github.com/snapcast/snapcast)


### 2026-09-06 v48交互修复补充

原生音量改为0～100整数百分比软件增益，主机协议保留生成消息及实体键；本地提示和HA回答共用设置，HA媒体播放器内部仍为0～1。语速新增设备绑定的中文绝对/相对控制与查询，TTS缓存区分语速。新增只读“唤醒会话序号”诊断传感器，用于跨本地唤醒边界清除调节简称上下文。

原生起音采用120ms VAD与AC能量联合确认，默认等待20秒、尾停顿1.8秒、命令上限30秒不变。HA原生STT在正常EOF后检查音频能量与无效文本；该过滤不代表全部声学回声已解决。首次无输入静默退出，续听无输入只播结束提示。HA2026.8.2按集成分别归属设备，不再尝试通过旧多集成注册方式合并设备记录。详细依据、回退及实机验证边界见`docs/2026-09-06-r1-interaction-fixes.md`。第14章正式验收指标保持不变。


### 2026-09-06 v53用户追加决策

等待开口拆分为首次唤醒默认10秒、持续对话默认15秒，两项均支持HA与明确的中文语音配置，1～120秒整秒步长，R1持久化。原共享等待实体迁移为首次等待并保留身份，新添续听实体。全部收音计时与起音/停顿状态机在R1执行；HA仅管理配置及执行提交后的STT、对话、TTS，不运行原生链路外部VAD分段。

自然交互要求为本地回应/续听提示结束后立即可以说指令，不要求用户等待1秒。v53在播放本地提示前预先打开并持续读取麦克风，丢弃播放期间输入，将完成边界的第一段音频移交收音，取消回应后的固定200ms休眠与开麦间隙。用户暂时离开，真实录放并行及立即接话效果待返回后验证，不据单元测试宣称实机完成。声学和长期验收指标不变。


### 2026-09-06 v54 交互修订

用户确认持续会话取消开始提示，TTS实际播放结束后直接进入R1端持续等待。首次等待默认10秒，持续默认15秒，检测到有效开口后切换既有讲话完毕检测；HA仅配置这些端侧参数。首次无输入和持续等待结束均播放一次结束语。STT空识别恢复同一窗口剩余时间，不重置完整预算，不把服务错误当静默。播放期间持续采集并丢弃，结束边界输入有界保留。v53曾复现提示结束后误触发并提前退出；真人复测现由固定输入、时钟事件和状态回读回归取代。详见`docs/2026-09-06-r1-continuous-listening-v54.md`。


### 2026-09-06 v55 首次提示边界修订

v54 真人首次静默仍在提示后约 120 ms 误触发空识别，恢复剩余时间机制实际生效，但误触发未解决。v55 Native 本地提示统一使用 AudioTrack PCM 写入与播放头消费完成边界，不再用 MediaPlayer 完成回调作为该路径的输入起点。继续保持录音、丢弃播放期间帧并保留结束边界后的输入，不增加固定禁听延迟、不提高全局开口门槛。HA 仅对完整语速控制句式兼容“语数”；无全局天气/数学词替换。相应录音播放能力须重新实机验收，细节见 `docs/2026-09-06-r1-prompt-v55.md`。


### 2026-09-06 v56 本地提示参考匹配

v55 真人静默 5 轮中第 5 轮出现非空“我。”及无意图回复，维持失败判定。v56 引入仅限本地提示的 2 秒有界播放参考，最多 500 ms 延迟搜索，连续 3 帧 .85 相关性且延迟一致才减去估计的线性播放分量；检测/预录/上传统一使用残差，低置信保留原输入。提示结束 1 秒后清空参考，不设置固定禁听窗口。诊断仅含统计和时间，不保存家庭音频。这不是完整房间 AEC，需实机证明残留抑制、短句/立即接话及处理预算；见 `docs/2026-09-06-r1-reference-v56.md`。


### 2026-09-06 v59 定向诊断补充

v58 静默误触发仍失败，具体声源未定。v59 新增默认关闭、显式开启且最长 30 秒的本地诊断，记录原始/处理后 PCM、本地提示参考及单调时间线；沿既有 shell 授权的 ADB 本地接口校验导出，不新增网络服务。采集有界，音频不提交仓库，失败不覆盖，用户选择先做明确的静默窗口。仅补齐定位能力，不改 VAD/参考处理语义或第 14 章验收指标；实机诊断开销、故障归因和修复效果仍未验证。实施及后续入口见 `docs/2026-09-06-r1-audio-diagnostic-v59.md`。


### 2026-09-06 v59～v61 静默诊断结果

用户确认的 v59 六轮本地管理窗口均未误上传，但一轮诊断入队墙钟耗时 41.624 ms，诊断性能失败。v60 改进诊断队列并增加分线程复制/入队耗时及超预算自动停止；追加一轮静默行为和诊断预算通过，不能外推长期性能。以上七轮绕过 Alexa，且未覆盖历史 v58 失败使用的 ack:1；v61 新增仅用于本地管理诊断的固定提示索引，正常随机回应不变。后续先定向复现，再决定处理模型；v58 故障原因及修复均未确认，第 14 章指标不变。详见 `docs/2026-09-06-r1-diagnostic-queue-v60.md`。


2026-09-06 v61 固定 ack:1 单轮追加结果：本地接口触发、用户确认静默，完整等待后一次结束语，无上传/STT/TTS；诊断复制/入队最大 2.244 ms、参考处理观测最大 15.675 ms，本轮未超预算。历史约 1.92 秒的迟发输入仍未复现；真实 Alexa 交互未由本轮覆盖，故障原因和修复仍未确认，不改变第 14 章门槛。证据见 `docs/2026-09-06-r1-diagnostic-queue-v60.md`。


### 2026-09-06 v62 五轮真实 Alexa 首次静默检查

v62 在同一台 R1、当前配置和诊断开启条件下完成五轮真实 Alexa 唤醒后静默：5/5 无误上传/STT/TTS，均等待约 10 秒后一次结束语，覆盖 ack:5/0/2/7/1。诊断复制/入队最大 4.122 ms、参考处理观测上限 15.005 ms，未观测到超预算；全部录音校验后清理设备副本，诊断已关闭。这是首次静默子项的本组通过结果，历史迟发误触发仍未复现、原因与针对性修复未确认，不代表生产总验收通过。后续为立即开口、短句、持续接话与静默，再按第 14 章验证网络恢复及长期稳定性。详见 `docs/2026-09-06-r1-wake-triggered-diagnostic-v62.md`。


### 2026-09-07 短句语义失败与 HA 路由修复

v62首次回应后立即开口、连续接话及短句的真人结果保留为历史诊断，其中“是→4”、“好”确认分流和提问构造偏差未形成标准声学结论。自2026-09-12起不再安排真人复测；对应上传/STT/TTS事件、确认上下文、权限和状态机使用固定输入及真实后端契约验证。历史问题不改写为通过，也不阻塞网络恢复与72小时自动验收。证据见`docs/2026-09-07-r1-short-reply-failures.md`。


### 2026-09-07 用户决策：暂缓调试与验收，优先完成功能

用户明确将意图/语音链路调试、断网/IP 变化恢复检查及 72 小时稳定性验收暂缓，待单台功能实现齐备后统一进行。当前开发与必要检查按第 13 章新增的执行顺序开展；本决策覆盖此前报告中立即继续复测或稳定性验收的排期，不覆盖历史失败、通过结果和未验证状态，第 14 章指标不变。当前入口见 [README](./README.md#当前开发顺序)。

## 2026-09-07 开发流程补充

源码采用 GitHub 公开仓库管理。Linux 无设备环境可完成实现与主机检查，设备验证绑定提交与 APK 哈希后执行；代码支持、主机通过、实机通过继续分开记录。hostcheck 包只用于构建，不是可部署候选包。详见 `docs/development.md` 与 `docs/device-validation.md`。不改变第 14 章指标或既定验收顺序。
