> 历史记录：本文版本、命令及“下一步”反映记录当时状态；当前排期以[根 README](../README.md)为准。原始产物迁移见[归档说明](../test-results/README.md)，原验收结论不变。

# 模型训练及候选产物清理（2026-09-05）

用户明确要求清理模型训练和测试时无用的文件目录，仅保留使用中的模型。
已按此要求删除旧候选产物；未操作 R1 或 HA 上的文件/系统包。

- 删除 107 个目标、138933 个文件。目标文件占用约 25.57 GiB，
  文件系统可用空间增加 27539492864 字节（约 25.6 GiB）。
- 删除全部 local-models/camila：训练/合成/录音/分段数据、特征、
  checkpoint、openWakeWord、microWakeWord 旧模型和 sherpa 候选。
- 删除 Camila 各训练虚拟环境、KWS 验证环境、CUDA 兼容缓存、
  不再使用的训练/候选源码与本地构建、旧模型打包目录、未选用的 TFLite 版本。
- 删除旧训练/比较脚本及其专用测试、PocketSphinx/openWakeWord 探针、
  sherpa 构建工具与 Gradle 依赖；移除新建但已停止的 20 轮验证工具入口。
- 清除旧 Android 构建目录，随后从保留依赖干净构建当前版本 35。
- 保留当前 Alexa 权重与上游清单/LICENSE、TFLite 2.10.0、
  r1-microfrontend 运行库及其固定版本构建源码（整个 local-deps 约 8.5 MiB）。
- 保留当前运行/安装工具、代码单元测试、阶段 0/音频备份和文字验收记录。
  已删除候选的历史文字报告仍有效，但其中依赖已删数据的回放/训练命令不再可执行。
  此处未删除早期音频底层验收样本或设备恢复材料。

保留的 Alexa 模型 SHA-256：
`9011a8155b04de858c48038529235cbc0e42e9fca05a55bf588cb80a653a723b`。

[删除清单和保留哈希](../test-results/README.md)
记录具体目标、文件数量和空间占用。删除前后 Alexa 权重、
microfrontend 运行库及两份当前安装音频修改包的 APK 备份哈希一致。
没有创建大体积清理备份副本；这些旧训练产物已实际删除。

清理后的 36 项主机单元测试、lint 和干净构建通过。生成 APK 仅包含选定 Alexa 权重，
不含 sherpa/PocketSphinx/openWakeWord 旧候选运行库。未再次进行真人 KWS 测试，
未将新构建安装到 R1，设备仍使用先前已安装的版本 34。
