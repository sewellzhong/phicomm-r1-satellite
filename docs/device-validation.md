# 实机待验证清单

状态取值：待验证、通过、失败、阻塞。代码支持、主机检查与实机结果分别记录；`main` 是开发主线，不是已验收固件。

| 项目 | 源码提交 | 主机结果 | 实机状态 | 检查步骤与预期 | 风险/证据 |
| --- | --- | --- | --- | --- | --- |
| Git 整理后的家中 APK 构建与素材路径 | 首次 Git 整理提交（执行前用 `git rev-parse HEAD` 固定） | 见仓库整理报告 | 待验证 | 用原签名/中文素材构建；核对包名、版本、APK 哈希和素材，再按既有部署流程做必要最小检查 | 本次不部署；旧 v63 结果不自动继承 |

新增功能复制以下记录，填写实际值后提交；没有结果时填“未运行”，不可留成通过。

- 功能与范围：
- 受测源码完整提交 SHA、是否有未提交修改：
- 主机命令与结果：
- 实机状态、日期、设备代号、固件：
- APK 版本、SHA-256、签名核对结果：
- 前置条件、步骤、预期结果：
- 实际结果、失败/风险及脱敏证据链接：
- 本地备份/回退基线是否核验（不填凭据）：
- 下一步：
