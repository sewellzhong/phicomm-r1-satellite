# v129 对话连续性修复主机记录

日期：2026-09-14

## 结论

用户报告真实语音对话播放断续，且R1会把自身回答当作新命令持续自问自答。v129主机候选完成两项有界修复，但尚未连接生产HA或ADB，也未更新`r1-sample01`，所以当前结论仅为代码和主机门禁通过，真实声音与循环终止保持待验证。

## 实现

- PCM仍固定为S16LE、16 kHz、单声道。长流在AudioTrack启动前等待6,400字节，即200 ms；已经结束的短流直接播放已有数据，不补零、不等待不存在的数据。32 KiB环形缓冲、背压、300秒总超时和取消释放边界不变。
- 新增启动缓冲字节/等待时间、播放参考抑制帧、排空后尾音抑制帧、当前连续轮次、固定上限及上限终止次数诊断；不保存录音或问答正文。
- 播放参考候选相关阈值从0.85调整为0.72，但仍须连续三帧、延迟偏差不超过320采样才减去检测副本；Assist上传继续使用未经修改的原厂PCM。该参数是待实机验证的候选，不命名为原厂标准或AEC效果。
- 保留每次正常回答后的免唤醒续听。一次Alexa唤醒最多接受10轮命令；第10轮回答后只播放一次既有结束提示并回到待唤醒。新Alexa、无输入窗口超时、用户停止、计时器抢占、失败、断线和服务重启清零；播放中直接插话计入同一连续会话。
- Android候选版本为129，名称为`1.29-dialogue-continuity-fix`。

## 主机验证

定向Android测试覆盖200 ms启动门槛、短流、100 ms突发供给、预缓冲取消、完整排空、播放参考失真、混合真人信号不被抹除及10轮上限。Android完整单元测试共288项，0失败、0跳过。

统一执行：

```bash
ANDROID_SDK_ROOT=/home/sewellzhong/.local/share/android-sdk python3 tools/dev/prepare.py
ANDROID_SDK_ROOT=/home/sewellzhong/.local/share/android-sdk bash tools/dev/check.sh
```

结果：完整门禁执行时公开文件扫描588个/0发现、凭据扫描0；Android单测、lint和hostcheck构建通过；Native工具77项、恢复73项及演练、原厂音频117项、更新CTest 6项及工具41项、系统控制、HA固定2026.8.2镜像107项与生命周期均通过。加入本记录后最终复扫为公开589个/0发现、凭据0。hostcheck APK SHA-256为`4307c4d96133d641fd5987a41ed7049b664c92a1fcee4e97293ae2a464854ce5`，含合成提示素材及独立包名，禁止部署。

## 实机入口

用私有中文提示素材和既有设备签名构建v129，核对当前`r1-sample01`仍为3448/API22、v128、Enforcing、生产包名、签名、APK哈希、原厂包隔离与回滚基线，再走现有监督更新路径。验证短/长真实回答听感及欠载、静默续听不产生第二个HA run、至少三轮自然接话、回答尾部立即接话、播放中Alexa/直接插话、10轮后无第11窗口和服务重启恢复。保存事件和计数，不保存家庭录音或问答正文；失败时回滚v128并保留失败证据。
