# R1原厂方案与功能/数据/链路验证边界（2026-09-12）

## 用户决定

当前首台及后续开发不再要求用户充当测试人员执行受控人工验收，包括控制距离、方向、
声压、说话强度、话术、房间声学或主观听感，也包括为验收反复操作手机、按键或其他物理
步骤。旧报告中的相关结果继续作为当时环境下的历史诊断，不删除、不改写为通过，也不
继承为新版本结论。

这些项目统一记为`skipped_by_user`或“用户跳过/未验收”，不再阻塞功能开发。跳过不是
通过，也不是证明原厂声学效果达到某个数字。

## 原厂方案的含义

当前3448固件没有随仓库提供可审计的原厂四方向容差、唤醒率、识别率或主观音质验收
规范。因此“按原厂方案实现”固定指：

- 保留并实际调用设备内原厂Android内核、Audio HAL、四麦库、MicArray、DOA、AEC、
  AK7755 DSP固件和校准配置，不用自研声学算法替换其输出。
- 原样传递原厂DOA 0～359度及其运行状态，不按项目期望方向重映射；原厂只提供配置或
  状态而没有效果判定接口时，如实报告配置/数据状态，不推导性能通过。
- 公开仓库只实现自有代理、协议、状态机、审计和恢复工具，不分发原厂二进制、固件、
  校准、私有录音或凭据。
- 只有取得可审计的原厂规范或专业受控实验材料后，才可新增对应效果判定；不得把项目
  自定容差写成“原厂标准”。

## 强制验证

| 范围 | 必须证明的结果 |
| --- | --- |
| 来源与调用 | 设备/3448身份匹配，原厂文件和配置哈希匹配，实际命中原厂HAL/MicArray链而非仅保留文件 |
| 音频数据 | PCM格式、采样率、通道/长度契约正确，原始与处理载荷非空，帧数、序列和哈希一致 |
| 运行完整性 | 无真实ABI异常、异常生产线程、队列溢出或未解释丢帧；资源有界，停止后释放 |
| 协议与功能 | 唤醒事件、上传、STT/意图/TTS事件、播放/暂停/停止/打断、闹钟、播报、媒体、配置与状态按协议和状态机工作 |
| 故障与恢复 | 认证失败关闭，离线/错误不报成功，服务/整机重启、IP变化、看门狗、更新失败和回滚恢复真实状态 |
| 安全与隐私 | Noise认证、凭据不泄露、物理静音不可远程绕过、生产无公网ADB/公开root/全局Permissive |
| 稳定性 | 自动化长时间运行记录丢帧、CPU、PSS、温度、重启和连接恢复；最终候选执行72小时检查 |

硬件相关能力仍须在真实`r1-sample01`上证明功能和数据确实运行；可使用合成音、固定文件、
ADB或本地代理、协议驱动、事件注入、状态回读、哈希校验和自动化长稳，不以纯主机模拟
代替设备结论。无法自动产生的物理输入保留已有实机证据或标记人工验收跳过，不再请求
用户配合；对应软件处理、状态和失败路径仍须自动验证。

## 不再要求用户验收的项目

- 一米固定短语、声压/音量一致性、语音相对静音dB及主观清晰度/自然度回听。
- 四方向DOA准确率、各麦克风独立定向刺激、声源定位和商业阵列效果比较。
- 真人Alexa命中率、误唤醒率、中文识别正确率、特定短句修复复测。
- 真人在TTS或音乐中插话、真人语音保留、AEC听感、降噪/增益音质评价。
- 需要用户重复固定动作并由主观判断“好坏”的其他声学体验项目。
- 为验收重复操作手机配对/配网、按压实体按键、移动设备或执行其他人工物理步骤。

其中可以机器测量的状态转换、播放停止时间、数据残留、参考数据、唤醒事件处理和错误恢复
仍属于强制功能验证；效果准确率和听感则保持未验收。

## 历史证据与当前结果

2026-09-12 v28/v88静默和四方向五个10秒窗口均为500帧，全部真实完整性计数为0，
四类MicArray sidecar非空，采集后恢复监听和Enforcing。严格相对方向矩阵因非受控材料
失败，报告SHA-256为`54f0d133274547f1295c5853e4445bbbf2929714ef461f97ba5dfed3b087c937`，
保留为历史诊断。

`vendor_native_diagnostic`模式对同一批材料只验证原厂DOA数据存在、单次严格审计和链路
完整性，不做方向映射或残差判定；结果`pass`，同时固定输出
`directional_acceptance=skipped_by_user`。私有报告SHA-256为
`43715fe969472fba5bb66670b0fee23345c31adcded303618a41aa1444482da5`。

## 对旧文档的统一取代关系

所有2026-09-12以前文档中的“下一步真人复测”“固定距离/音量重录”“四方向矩阵验收”
“用户回听选型”“真人唤醒率/识别率验收”和类似要求，自本决定起不再是执行入口。历史
章节保留原貌用于审计；当前排期、状态和验证结论以本文、根README、AGENTS.md及
`docs/device-validation.md`为准。
