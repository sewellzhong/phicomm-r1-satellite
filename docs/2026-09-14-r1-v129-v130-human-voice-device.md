# R1 v129/v130对话流畅度与循环真人验收

日期：2026-09-14

设备：`r1-sample01`，3448 / API 22 / SELinux Enforcing。

> 2026-09-16 复核发现本阶段设备摆放时麦克风朝下。本报告的播放中打断、方向/声学和体感相关结果保留用于审计，但受姿态影响，不能直接代表正确摆放能力，须按[姿态纠正与真机验收恢复决定](2026-09-16-r1-repositioned-microphone-revalidation-plan.md)重新验收。

## 结论

用户报告的播放卡顿在v129/v130真人问答中未再出现，所有受测播放`underrun=0`，用户明确反馈回答流畅。v129仍会把10%音量的自身回答错误判为无需唤醒的直接插话并形成循环；v130将该未验证入口失败关闭后，用户确认回答后安静期间不再自说自话。

本轮整体真人功能验收未通过：播放中说`Alexa`没有停止当前回答，设备计数同样为`reply_wake_interruptions=0`。无需唤醒的播放中直接插话因v129真实失败而在v130保持`blocked`，不能算打断能力交付。用户另反馈反应偏慢；状态切换测得本地唤醒回应约1,557 ms、端侧进入processing后约883 ms开始播放，当前1,800 ms讲话结束停顿是主要可调等待。

## 更新与身份

- 更新前现装v128两次拉回均为`796cbb10db66e06ac3c10997ed3eba565be9d12f07a4bd27b863a4ac29ee0ce7`，与既有本地基线一致。
- v129候选及安装读回均为`1c5937ceb6588c84b7a46a6954c60b324c758a5d5042337039a38c1bbbaf57b7`。
- v130候选及安装读回均为`a3df0f686bfd3d33c9f92ba3bf42b6401afd42b959ffc638c8c1b6acbff58868`。
- 两版签名均为`0be7a3643442658354c185ec53cb50e73bc1516a56f2760ca46f2c932c1d2639`，由独立监督器更新并恢复四项健康；未修改boot、system、recovery、Loader或分区表。
- v130完整主机门禁通过：公开589/0、凭据0、Android 288项/构建/Lint、Native 77项、恢复73项及演练、原厂音频117项、更新CTest 6项和工具41项、系统控制、HA 107项及生命周期；加入本记录后最终复扫590/0、凭据0。

## 真人与自动状态

v129首次自然问答在一次唤醒后由1条真人命令增长到5条命令，产生4次`direct_barge_interruptions`；播放参考仅匹配少量帧，无法阻止自身扬声器回声进入直接插话。操作者通过本地按键语义停止，设备恢复`listening`，该失败原样保留。

v130保留回答排空后的自动续听，但播放期间只接受Alexa唤醒打断；回声候选计入`direct_barge_rejections`而不再提交。单轮问答为1次唤醒、1次命令、1次STT和1次完整播放，启动缓冲6,400字节、等待81 ms、underrun 0；回答后10秒静默没有新增命令，窗口正常超时并回到`listening`。用户反馈声音流畅且没有自说自话。

随后自然连续问答均保持underrun 0。播放中Alexa打断脚本未观察到新唤醒或取消，长回答自然播放完成，用户确认第一段回答没有立即停止，因此`playback_barge_in=failed`。最终设备为v130、`listening`、连接1、`audio_opened=true`、`last_error=null`、Enforcing；累计4次唤醒、10条命令、9次STT、8次完成、0次播放中Alexa打断和0次直接插话。

脱敏结果见`test-results/2026-09-14-r1-sample01-v130-human-voice/`。自然功能结果SHA-256为`e02ca70ead748892889b173562f859195585485ac419f9fd15a1620e2277845c`，受控诊断结果为`41338ac1b43a31bb5d1d847a5a20b857bbaa7e889a23f9c37c9aac14dd0bbfc1`；两者均通过结构校验，不包含话术正文、录音、凭据或家庭对话。

## 后续入口

保持v130作为安全诊断候选。响应速度优先通过HA/R1共用配置把讲话结束停顿从1.8秒改为1.2秒并真人复测截句风险，不先牺牲已证明流畅的200 ms启动缓冲。播放中打断需针对真实播放下Alexa未检出和原厂回声链继续修复；在新的实机证据通过前，不重新开放无唤醒直接插话，也不把本轮标为功能验收通过。

## v131/v132修复续测

v131把回答期间的Alexa KWS从原始麦克风帧移到与直接插话VAD相同的回声削减分析帧，候选及安装读回SHA-256均为`1e57c0ac2c5d42581097fb7c7260dbb6ae09734373caf1cb3cd5652455cb09fc`。真人再次在长回答中说Alexa，用户确认仍未停止，设备计数保持`reply_wake_interruptions=0`；本地按键停止后恢复监听。该轮参考匹配122/2992帧，9帧超过20 ms预算，证明逐帧相关门槛在真实双讲下不足，v131继续记失败。

v132在已有三帧稳定延迟锁定后，把同一参考路径最多保持500 ms穿过突发双讲；它只从分析副本减去播放参考，不静音、不替换原厂上传PCM。新增主机测试证明突发独立语音保留在残差，纯回声不能通过直接插话判定；同时状态新增回答期KWS帧数、推理数、原始最高分、判定门槛和保持参考帧数，便于用非录音指标区分模型未听清与窗口不足。完整`tools/dev/check.sh`通过：公开590/0、凭据0、Android单测/构建/Lint、native77项、恢复73项及演练、原厂音频117项、更新CTest6项和工具41项、系统控制、HA107项及生命周期。

更新前v131安装包双读一致；v132生产候选SHA-256为`808733d1399470328e6605bf9eee43672c8d37290af7b944dd0ebff714d7364e`，签名仍为`0be7a3643442658354c185ec53cb50e73bc1516a56f2760ca46f2c932c1d2639`。独立监督器完成v132更新后，实际`base.apk`两次拉回均精确匹配候选；设备保持3448/API22、Enforcing、`listening`、连接1、`audio_opened=true`和`last_error=null`。更新后90秒观察期间没有自然唤醒或自行提交命令，但操作者尚未开始指定的真人播放中Alexa脚本，因此v132打断仍为“待复验”，不得继承主机结果或记为通过。

## v133～v138失败收敛与安全恢复

v132真人复验仍未停止回答，回答期KWS最高原始分仅2/255，远低于229门槛；这排除“小幅降低阈值”作为可靠修复。v133把强人声候选改为只取消当前回答且不创建新命令，避免循环，但安静真人验收中自身回声仍触发取消，只播放“太阳”两个字即停。v135～v137分别尝试暂停AudioTrack后二次确认、仅静音不中断写入，以及增加实际残差强度门槛；三版均在操作者明确保持安静时只播放相同两个字便停止。v135另证明直接暂停会使流式写入约0.49秒后释放；v136/v137则证明VAD挂起与约0.5秒设备链路延迟使同次静音确认不可用。这些结果全部记为失败，不以设备计数中的cancel覆盖真人事实。

v138撤销播放期VAD停播，保留回声候选计数并失败关闭直接插话，恢复v132类安全行为。Android全量单测、构建和Lint通过；此前v136完整统一门禁通过，公开扫描592/0、凭据0。v138候选SHA-256为`fe52f435b29c91e6aaebfa184b744037777789423ed0d7d5e8c1d67c15932028`。独立监督器在三个新操作ID及一次自身重启后均未执行替换，设备保持v137；该更新故障保留为失败。固定PackageManager维护安装首次因空间不足失败且未改变包，随后仅精确删除本轮v132～v138九个已固定私有候选副本，`/data`可用约258 MiB；v137已在主机双读保存且哈希一致。维护安装第二次成功，v138实际安装包双读均与候选匹配，设备为Enforcing、`listening`、连接1、音频打开且无错误。该维护恢复不算监督更新通过。

v138安静长回答的自动状态已越过此前约0.5秒误停点并持续`playing`，回声候选拒绝1次、命令保持1、underrun为0；等待用户确认完整听感。播放中语音打断仍未交付。下一实现不得再用播放期VAD直接取消或用同次静音确认，应先以只观测、不影响播放的短片段KWS重置重放取得真人与中文回声分数证据，再决定是否允许取消。

随后用户确认v138回答可以完整播放，但回答结束后保持安静时，尾音仍被识别成“我”，HA继续回复未听清并开启下一轮；设备累计命令也由1增长到6。因此v138只解决误停，没有解决续听自循环。v139将`automatic_followup_enabled`固定为false，回答结束后即返回Alexa待唤醒，不再开放免唤醒窗口；候选及安装双读SHA-256均为`fa2b60ad31e1ebce8c4ca31aab8512232038754136af4159b80fb965f12e8aa8`。一轮真实长回答的自动状态为1次唤醒、1条命令、1次STT、1条TTS、1次完成，播放underrun为0；自然结束后静默超过15秒，所有计数保持不变并停在`listening`。用户最终听感确认尚待返回，播放中打断和免唤醒连续对话均继续标记未交付。

用户随后确认v139没有自动下一轮，但指出播放结束后没有持续会话窗口。v140恢复自动续听，并在播放排空后先丢弃1.2秒尾音、重置VAD/KWS再打开10秒窗口；Android全量单测、构建和Lint通过，候选及安装双读为`55e8966e18c9f37c9b440035d73b179437f6e333f06b456a679c06d00519b965`。真实静默脚本确实观察到`settling_followup→waiting_followup`，但窗口仍自动把命令从1增至2，随后第二窗口才超时，故v140失败。设备使用已双读的v139 APK执行同签名显式降级，最终回读v139、Enforcing、`listening/audio_opened`、连接1且无错误。固定延时不能解决持续窗口误触发；v139安全单轮仍为当前基线，持续会话未交付。

## v141持续窗口准入修复

v141保留1.2秒尾音隔离，并把持续窗口的启动条件从200 ms分散语音/120 ms强语音收紧为300 ms分散语音或200 ms强语音；初次Alexa后的命令门槛不变。新增主机测试证明不足200 ms的短尾音不能启动，200 ms清晰强短回答和300 ms持续轻声仍可启动。Android全量单测、构建和Lint通过，最终完整`tools/dev/check.sh`亦通过：公开592/0、凭据0、native77项、恢复73项及演练、原厂音频117项、更新CTest6项和工具41项、系统控制、HA107项及生命周期。

v141候选及安装包两次拉回SHA-256均为`bc3779de21883ed537e6a457594c85a345dde7ab16131b1a7a5b0552952591bd`，签名沿用既有摘要。首轮真人长回答后保持安静，设备进入正式持续窗口但没有新增命令，窗口正常超时；用户确认没有自动下一轮。第二轮重新Alexa发起问题，回答结束约2秒后用户不再说Alexa、直接说“现在几点了”，设备命令从2增至3、STT从2增至3并正常回答；用户确认回答正确且正常结束。第3次回答后的持续窗口保持安静，命令维持3并正常超时回到`listening`，没有第4条命令，播放underrun为0。由此v141的静默不循环和免唤醒持续接话在首台通过；播放中Alexa立即打断仍维持失败，不能把本轮整体标为全部通过。

## v142～v145播放期Alexa观测

为避免重现v133～v137的静默误停，四个版本均固定关闭回答取消，只记录内存中有界PCM的Alexa推理结果。v142在VAD候选出现时重置KWS并重放最近2秒回声削减帧；静默和真人播放中Alexa各执行一次，重放均0命中、峰值0。v143增加同窗口原始原厂PCM重放，静默和真人对照仍均0命中、峰值0。v144把重放延迟40个20 ms帧，以覆盖完整短词；静默处理后/原始峰值为6/1，真人对照后的累计峰值仍为6/1，两路均0命中。该结果证明单个VAD窗口被播放回声抢先占用，不能代表完整回答期都没有收到真人声音。

v145另建独立KWS实例，对回答播放全过程的原始PCM连续推理，不改变上传PCM、播放音量或取消状态。4%设备音量的静默轮处理2606帧/1736次推理，0命中、峰值5、单帧最大2.046 ms；真人在回答播放约2秒后说一次Alexa，累计变为4621帧/3078次推理，仍0命中，峰值升至12、单帧最大7.802 ms。峰值变化证明真人声音进入观测输入，但仍远低于固定229门槛，不能通过降低阈值安全交付。两轮均`playback_underruns=0`，各自产生且完成一条命令，回答后持续窗口静默超时，没有额外命令。

候选及安装双读SHA-256依次为：v142 `f36f860874854152c9e1385a5dfe06f8ed76ad725dd3c165507013228728de04`、v143 `e6d56f1aa8c2d02939aea5038bc34aa41e463345844f52432f3582a826d3eac6`、v144 `12ea8783b0671b4cda9e5fa9b6f5d7144279c18ef107072a30059c79ae57424b`、v145 `b8ad55360225fd9cd7092d17c77d42d9796a109aff43ec8f862c92bf253ef1dc`；签名均为`0be7a3643442658354c185ec53cb50e73bc1516a56f2760ca46f2c932c1d2639`。v145保持3448/API22、Enforcing、`listening`、音频打开且无错误。v143～v145通过Android全量单测、Lint和设备APK构建。最终完整`tools/dev/check.sh`通过：公开扫描590/0、凭据扫描0，Android构建/单测/Lint、Native、R0恢复73项及演练、原厂音频117项、更新CTest及工具、系统控制和HA107项/生命周期均通过；HA检查仍明确为合成麦克风及非生产HA，不替代上述真机结果。

结论仍为阻塞：当前播放残留下，真人Alexa无法达到既有预训练模型门槛；VAD、暂停、静音、能量和降低KWS阈值均已有误停或不可区分证据。未经新的原厂AEC/参考处理输入或等价的可复核区分证据，不得启用播放期自动取消。v145只是诊断版本，不是播放中打断已交付版本。

## v146～v149原厂AEC续查

2026-09-15按用户选择的原厂AEC方案继续。离线审计和真机调用确认3448系统
`libaec.so`的构造、选项、1200字节块及`[mic,echo]`交织格式；APK不包含原厂库，诊断
入口在独立进程中且不连接生产取消。四路原始麦克风与两路同窗口硬件参考的16组矩阵
得到静默2～8、真人Alexa最高20；最佳输出2×增益后真人54、静默20/24，仍低于固定
229门槛，继续增益已削波。故原厂AEC“实际命中”已证明，但“足以安全检出播放中Alexa”
仍失败，生产取消继续关闭。完整结果见[原厂AEC实机记录](2026-09-15-r1-v149-vendor-aec-device.md)。
