# Phicomm R1 Voice Satellite 项目规则

## 项目目标

- 本项目用于将斐讯 R1 改造为 Home Assistant 语音卫星。
- 以 `斐讯R1语音卫星技术方案-2026-08-27.md` 为需求、架构、阶段门槛和验收指标的主要依据。
- 阶段 0 和阶段 1 音频子门槛已通过。用户于 2026-09-05 决定直接采用现有 Alexa 预训练模型，不再开展 KWS 训练、选型或声学测试，推进阶段 2 Assist WebSocket 集成。未完成的 KWS 正式验收保持“未验证”，此开发推进决定不等于阶段门槛通过或后续能力已交付。

## 当前开发与验收顺序（2026-09-07 用户决策）

- 暂停意图与语音链路调试、断网/IP 变化恢复检查和 72 小时稳定性验收；不依据旧报告的“下一步”自动恢复这些检查。
- 优先补齐首台 R1 的单台核心功能：播放中打断、流式回答、主动播报与计时器、本地闹钟、免打扰、媒体播放、手机蓝牙音箱、安卓/iPhone 自助配网、按键/物理静音/灯效、设备名称/区域与系统管理、签名更新与回滚。详细需求见 `docs/2026-09-07-r1-feature-requirements.md`。
- 功能实现齐备后，针对最终候选版本统一进行可自动复核的功能/数据/链路回归、网络/IP 恢复检查，最后进行 72 小时稳定性验收。开发期间仍执行必要的构建、单元测试及最小实机检查。
- 开发顺序调整不等于阶段门槛通过。“好”、“是→4”、提问理解偏差、历史偶发误触发等真人结果保留为历史诊断，不继承为当前版本结论。
- 当前范围仍是首台 R1；不扩大到多台部署、同步音乐实验，不重启已停止的 KWS 训练、选型或专项声学测试。

## 原厂方案与非专业真人验收调整（2026-09-12 用户决策）

- 当前首台及后续开发不再要求用户充当测试人员执行受控人工验收，包括移动/定位声源、控制音量或话术、判断听感、反复说词，以及为验收操作手机、按键或其他物理步骤；相关人工项目统一标记为“用户跳过/未验收”，不再阻塞功能开发。
- 历史真人、固定短语、方向矩阵、主观回听、唤醒率、识别偏差和插话体验结果原样保留为诊断证据，不改写成通过或删除，也不自动继承到新版本。
- 音频实现使用当前3448固件的原厂Android内核、Audio HAL、四麦库、MicArray、DOA、AEC、DSP固件和校准配置，直接保留原厂输出语义；没有原厂可审计规范的阈值不得自行命名为“原厂标准”。
- 强制门槛改为可自动复核的功能、数据和链路：组件/配置来源和哈希正确，原厂调用实际命中，音频及状态数据非空且格式正确，帧/序列连续，无真实ABI异常、异常生产线程或队列溢出，认证/失败关闭/资源释放/重连/重启/回滚符合设计。
- DOA保留原厂0～359度、直方图和集中度，不重映射；AEC保留原厂配置、参考和处理数据的真实状态，不以日志或非空数据宣称消除效果；KWS/中文链路只验证模型加载、事件、音频上传、状态转换和结果处理功能，不做用户真人命中率、识别率或主观体验验收。
- 仍需真实R1证明硬件相关功能和数据链确实运行；优先使用合成/固定文件、ADB或本地代理、协议驱动、事件注入、状态回读和自动化长稳测试。无法自动产生的物理输入保留历史证据或标记人工验收跳过，不再请求用户配合；网络/IP恢复、72小时稳定性、安全、隐私、签名更新/回滚及各核心功能行为继续是门槛。
- 详细现行边界见`docs/2026-09-12-r1-vendor-native-functional-validation.md`；旧文档的“下一步真人复测/声学验收”均由本决定取代。

## 原厂音频优先路线（2026-09-08 用户决策）

> 本节保留2026-09-08的路线与证据要求；其中拆机和永久修改前置边界已由下方2026-09-11免拆分级授权更新。

- 原厂四麦、MicArray、DOA、AEC、DSP和校准处理链攻关立即提升为最高开发优先级；先完成该音频架构，再继续播放、闹钟、免打扰等上层功能，避免返工。详细路线见 `docs/2026-09-08-r1-factory-audio-root-plan.md`。
- 允许对首台 `r1-sample01` 使用拆机、Android root、临时或补丁 boot、定制 recovery/system、专用 SELinux 策略、特权系统服务和刷机。复杂、耗时或需要高权限不构成降级理由。
- 成功必须证明卫星实际使用设备内原厂四麦、阵列、AEC和DSP处理结果；仅保留原厂文件、经过标准 Audio HAL 或成功调用只读接口不算完成。
- 当前标准 AudioRecord 和自研处理只作为对照及最终降级候选。临时/补丁 root、专用代理、定制 SELinux、boot/recovery/system、私有库代理、最小原厂服务桥和保留原厂音频组件的实验系统路线未全部留下可复现失败证据前，不得提出降级；降级仍需用户明确决定。
- 首台进行任何永久修改前必须完成完整 eMMC/分区备份、两份校验副本、低层恢复入口和一次受控完整回刷验证。该门槛通过后，用户已授权继续针对 `r1-sample01` 的 Root 和刷机实验；设备或固件变化时重新建立基线。
- 公开仓库只保存自有代码、接口、提取和校验工具。原厂库、APK、DSP固件、校准、完整备份和修改镜像保留在本地，不随仓库或 APK 分发。

## 首台 boot-only 风险豁免（2026-09-10 用户决策）

> 本节保留2026-09-10实验的历史授权；后续操作范围以紧随其后的2026-09-11免拆分级授权为准。

- 用户决定跳过拆机和物理 Maskrom，仅对 `r1-sample01` 接受因没有完整 eMMC、独立恢复入口和受控回刷而永久变砖或丢失数据的风险，允许继续 boot-only 原厂音频实验。
- 该决定不使 R0 通过，也不证明设备可恢复；R0继续为 `pending`。风险接受必须绑定当前固件 fingerprint、原 boot SHA-256、仅 `boot` 范围和明确风险，保存在仓库外。
- 首次永久修改只允许使用双份一致的原 boot 输入，保留原 kernel、DTB、second、地址和页大小，仅修改 ramdisk中的自有代理、init和专用 enforcing SELinux策略。禁止全局 Permissive，禁止同时修改 system、recovery、Loader、分区表或物理首4 MiB。
- 写入前必须从当前Loader把boot复读两次并匹配原始哈希；写入后、复位前必须读回并匹配候选哈希。工具、设备、地址、长度或哈希任一不符立即停止。

## 首台免拆分级授权（2026-09-11 用户决策）

- 本节取代上述boot-only豁免作为后续开发的现行授权边界，但不改写2026-09-10实验的历史范围和证据。详细决定见 `docs/2026-09-11-r1-no-disassembly-authorization.md`。
- `r1-sample01` 始终不拆机、不短接、不焊接，不把物理Maskrom作为开发或交付前提。授权只绑定当前首台和当前3448固件基线；设备或固件变化时重新盘点并重新授权。
- 为完成既定功能，在较低权限路线已有可复现实机证据证明不足后，允许必要的Android root、boot、system、recovery、特权系统服务、最小原厂服务桥、系统包管理和专用Enforcing SELinux策略；允许刷写精确命名的boot、system或recovery分区。每次仍须执行身份/范围核对、目标分区双读、候选固定、单次写入及复位前完整读回校验。
- Loader、参数/分区表、物理首4 MiB、擦除、格式化、整盘覆盖和其他设备仍未授权；发现确有必要时必须单独提交证据、范围和风险并取得明确授权。生产镜像禁止全局Permissive、公开root入口和公网ADB。
- 电气硬断麦和所有引导组件同时损坏后的恢复均为非强制项。必须交付实体按键触发、特权层停止采集、远程不可解除、状态/灯效真实且重启保持的强制软件静音；没有电气证据不得称为硬件断麦。不得为恢复验收故意破坏全部引导链。
- R0继续为`pending`，风险接受和功能交付均不等于完整恢复门槛通过。若免拆路线出现不可实现的硬件限制，提交可复现证据和影响由用户决定，不得静默删减功能、降低指标或宣称完成。

## 配置、状态与新增功能要求（2026-09-07 用户决策）

- 必要配置和状态像音量、语速一样支持语音与 HA 双入口；可写配置可查询/修改，只读状态可查询/展示。共用配置与确认结果，失败、离线、待同步不可反馈为成功；不把底层工程参数全部开放为日常设置。
- 闹钟在 R1 本地保存和执行，语音及 HA 均可创建、查询、修改和取消；全部已有闹钟必须在 HA 显示，支持同步状态、启停、重复日期和响铃设置。已同步闹钟不依赖 HA/网络在线；语音管理仍使用 HA 中文链路，不新增离线语音识别。
- 免打扰支持手动和时段配置并在 R1 本地执行；默认屏蔽普通主动播报、降低灯光，保留主动唤醒和闹钟，闹钟例外可配置。任何入口不得绕过物理静音或泄露凭据。
- 自助配网覆盖安卓/iPhone，优先验证热点网页方案，不满足条件时验证并采用蓝牙配网；保留维护兜底。原厂音频路线允许 Root/刷机不等于配网故障可绕过安全门槛或自动改写系统。手机蓝牙音箱须独立验证原包隔离后的接收与音频共存能力。
- HA 对接内容平台，支持视频按名称、搜索、随机或评分筛选后在目标电视播放；具体平台接入后续在 HA 实施，不阻塞 R1 基础功能。当前不增加蓝牙网关或 R1 本地视频解码；服务重启与整机重启必须区分。

## 工作原则

- 优先完成首台可退换 R1 真机的开发验证，再规划或实施 3～8 台批量部署。
- 优先保留并实际复用原厂 Android 内核、Audio HAL、四麦库、DSP 固件和校准数据；通用 Linux 排在原厂 Android、特权代理和定制系统镜像之后，仅在保留原厂音频组件的前提下作为末端实验路线。
- 区分“代码或协议已支持”“模拟测试通过”和“R1 实机验收通过”，不得用前两者代替真实设备结论。
- 遇到方案与实机证据冲突时，以可复现的实机结果为准，并同步记录偏差、影响和决策。
- 不扩大当前任务范围；严格同步音乐和左右声道属于独立实验，不阻塞语音卫星首版。

## 实机与数据安全

- 停止或替换原厂系统包、获取root、刷机或改写分区前，必须先确认目标设备和当前固件并建立与目标范围相称的可验证备份；`r1-sample01`按2026-09-11免拆分级授权可在R0 pending时例外推进精确命名的boot/system/recovery操作。
- 当前提前授权仅覆盖当前首台及当前3448基线的必要root、boot/system/recovery、特权服务和专用Enforcing策略。Loader、参数/分区表、物理首4 MiB、擦除、格式化、整盘覆盖、其他设备和批量刷机仍须另行授权。
- 原厂音频攻关前仍以当前独立包名和 v79 回退基线保持设备可用；专有原厂文件从设备读取，不覆盖或公开分发原厂 APK。
- 不在仓库、APK、日志或示例配置中保存 Home Assistant 长期令牌、Noise PSK、Wi-Fi 密码或其他凭据。
- 采集诊断音频时明确标注来源、时间和用途；默认不提交包含家庭对话的录音。

## 实现约束

- Android 端默认兼容 Android 5.1 / API 22；使用更高版本 API 时必须提供 API 22 兼容分支并在实机验证。
- 控制 APK 常驻资源占用，优先使用轻量依赖、Protobuf Lite 和 `armeabi-v7a` 可用组件。
- 音频主格式固定为 PCM S16LE、16 kHz、单声道、20 ms/帧；改变格式必须说明原因并更新方案与测试。
- 唤醒词固定为英文单词 `Alexa`（使用现有预训练模型，不再训练 Camila），不使用 `Hi`/`Okay`/`Hello` 等前缀；唤醒后的 STT、对话和 TTS 固定为中文。
- ESPHome Native API 实现必须固定并记录兼容版本，从对应提交的 `api.proto` 生成代码，不手写消息编号。
- 网络协议和配置不得默认关闭认证；生产连接使用 Noise PSK，不在源码中硬编码密钥。
- 对 KWS 框架、模型权重、词表和训练数据分别核对许可证；公开或商业分发前必须保留可审计记录。

## 文件与证据组织

- 项目说明和当前进度维护在 `README.md`；架构或验收边界变化时同步更新技术方案。
- 后续代码、工具和证据分别放入含义明确的目录，例如 `android/`、`tools/`、`docs/` 和 `test-results/`，不要把临时产物堆放在项目根目录。
- 实机盘点、音频样本、性能数据和验收报告使用日期及设备 ID 命名；不得记录可用凭据或不必要的个人信息。
- 临时构建产物、下载模型和本地密钥加入忽略规则，不提交到版本库。

## 验证与交付

- 每次变更执行与风险相称的构建、静态检查和测试，并报告实际运行的命令、结果及未验证项。
- 涉及录音、播放、AEC、KWS、后台存活或硬件控制的能力，必须在真实 R1 上验证后才能宣称完成。
- 性能、安全、恢复和稳定性按现行技术方案执行；第14章中依赖受控真人或主观评价的声学指标保留原数值但标记“用户跳过/未验收”，不得改写为通过。功能或数据链失败仍记录失败，不得以本决定掩盖。
- 完成阶段性工作后更新 README 状态，列出已完成项、证据位置、已知问题和下一阶段入口。

## 开发流程与 Git 管理（2026-09-07）

- Linux 无设备环境可以完成独立功能实现、主机测试、提交和 PR；实机相关能力记为“待验证”，不因没有 R1 停止可独立开展的开发。不得自动连接实际 HA/ADB。
- 统一入口为 `python3 tools/dev/prepare.py` 和 `bash tools/dev/check.sh`。公开构建使用 hostcheck 独立包名与合成测试音，不作设备安装包。
- `main` 可包含主机检查通过但实机待验证的功能。交接记录见 `docs/device-validation.md`，受测提交和 APK SHA-256 必须对应，不将新提交继承为已通过。
- 原厂备份、回退 APK、中文提示素材、签名与私有 HA 路由测试源码保留在本地；不得提交到公开仓库。Git 发布前运行 `python3 tools/dev/audit-public.py` 并审查证据与第三方来源。
- 最终候选按功能/数据/链路自动回归、网络/IP恢复、72小时稳定性顺序执行；非专业真人验收不再安排。开发流程安排和用户跳过项均不代表阶段门槛通过。
